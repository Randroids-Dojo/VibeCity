# 14. Citizens

**Status:** partial

Citizens are the agents that move into zoned residential space, work in commercial / industrial space, and produce the visible street-level life the player sees from inside the car. Without citizens, the city is geometry; with them, it is a place.

This file is the canonical spec for the citizens layer (REQ-075 through REQ-079).

## What this section covers

- **REQ-075** Citizen population. Each residential zone cell has a capacity (small-house = 4, mid-house = 12 per existing building taxonomy). Population is an integer per cell, never per-citizen-instance for v1. The "citizens" the player sees are render proxies, not addressable agents.
- **REQ-076** Citizen pedestrians. Sidewalk-adjacent residential and commercial cells spawn ambient pedestrian sprites that walk between cells. Pedestrians are pure render; they have no goals, no schedule, no path-finding. Density mirrors population.
- **REQ-077** NPC vehicle traffic. The existing `ambient AI traffic` dot ships as the citizen-vehicle layer: cars spawn at residential zones, follow placed streets, despawn at commercial / industrial zones. Loop closes; rerun. One vehicle per "trip" demand event from the population layer.
- **REQ-078** Trip demand. Each tick, a residential cell with population > 0 generates trip demand toward the nearest commercial / industrial cell. The trip demand is a single integer counter on the cell; the vehicle layer reads it and drains it as cars complete trips.
- **REQ-079** Population growth and decline. Residential zones grow toward capacity at a rate gated by available services (REQ-100 series), employment (REQ-080 series), and power (REQ-085 series). With no constraints, full capacity in ~30 in-game days. With constraints, growth slows or population leaves.

## Schema sketch

```ts
City.sim.population = {
  // Keyed by "row,col". Only present for cells with a residential zone.
  cells: Record<string, { residents: number; tripDemand: number }>
  totalPopulation: number
  totalTripDemand: number
}
```

## Out of scope for this section

- Per-citizen names, identities, schedules, social networks (out: violates the proxy-not-agent stance for v1).
- Citizen happiness as a per-citizen state (citizens are integers, not records; happiness is a per-cell average).
- Voice acting, citizen dialog (no text bubbles, no audio in v1).

### Build log

- 2026-05-07: REQ-075 mass-appeal slice 1: population milestone toasts. `PopulationBucketSchema` adds `highestMilestoneReached` + `lastMilestoneTick`. `POPULATION_MILESTONES = [4, 12, 40, 100, 250, 500, 1000]` and `MILESTONE_TOAST_TICKS = 16` (`src/lib/sim/state.ts`). `syncPopulationToZones(population, zones, tick)` now records the highest milestone crossed when `totalPopulation` clears a threshold; decline that drops below does NOT clear the record. `EditorClient` mounts a green "milestone! N residents" toast. Tests: 3 new unit cases + 1 new e2e. Verified `npm test` (2173/2173), `npm run build`, `npm run check:dashes`, `npx playwright test e2e/sim.spec.ts --project=chromium` (29/29). Closes the dopamine-hit gap from the 2026-05-07 mass-appeal analysis. PR #N.
- 2026-05-06: REQ-079 per-abandoned-cell happiness penalty (oscillation damping). Files: `src/lib/sim/state.ts` (new `ABANDONED_CELL_HAPPINESS_WEIGHT = 6` and `MAX_ABANDONED_HAPPINESS_PENALTY = 60` constants), `src/lib/sim/events.ts` (`computeCityHappiness` counts abandoned cells inline (zone density 0 + population entry present) and subtracts the capped penalty from the score). Tests: `tests/lib/sim/events.test.ts` adds 1 new "abandoned cells damp happiness so the post-decline oscillation slows" case. Verified `npm test` (2169/2169), `npm run build`, `npm run check:dashes`, `npx playwright test e2e/sim.spec.ts --project=chromium` (28/28). Closes the post-decline-oscillation followup from PR #133: a city with many abandoned cells now stays in the stagnant band even when no residents remain to suffer the other penalties. PR #N.
- 2026-05-06: REQ-079 abandoned-cells HUD readout landed. Files: `src/app/[slug]/edit/EditorClient.tsx` (new inline span next to fire-risk readout, mounts when `abandonedCellKeys.size > 0`, carries `data-testid="editor-sim-abandoned"` + `data-sim-abandoned={count}`, color matches the gray dashed stroke from the per-cell visualization). Tests: `e2e/sim.spec.ts` adds 1 new "abandoned-cells HUD readout surfaces freshly-declined residential cells" case (paints residential, drives tax to 50% via 43 slider clicks, runs at 4x, asserts the readout mounts within 12s). Verified `npm test` (2167/2167), `npm run build`, `npm run check:dashes`, `npx playwright test e2e/sim.spec.ts --project=chromium` (28/28). Pairs with PR #144's per-cell overlay: count + which-cells together. PR #N.
- 2026-05-06: REQ-079 abandoned-cell visualization landed. Files: `src/app/[slug]/edit/SnapGridView.tsx` (new optional `abandonedCellKeys` prop; zone overlay carries `data-zone-abandoned={true|false}` and overrides stroke to gray dashed (`#8a8a8a`, dasharray `4 3`) when set), `src/app/[slug]/edit/EditorClient.tsx` (new `useMemo` deriving the set from `zone.density === 0 && population.cells[key] !== undefined`; passed into SnapGrid). Verified `npm test` (2167/2167), `npm run build`, `npm run check:dashes`, `npx playwright test e2e/sim.spec.ts --project=chromium` (27/27). The cue distinguishes never-grown density-0 cells (no population entry) from freshly-declined density-0 cells (population entry persists at residents=0 until next growth interval re-grows the cell). PR #N.
- 2026-05-06: F-014 follow-on: density-tiered pedestrian count. `src/app/[slug]/ambientPedestrians.ts` adds `pedestrianCountForResidents(residents)` tier table (0->0, 1..4->1, 5..12->2, 13+->4 capped at `PEDESTRIANS_PER_CELL_CAP`); `pedestrianAnchors` calls it instead of the prior `Math.min(residents, 4)` so density-1 cells render 1 figure, density-2 cells render 2, density-3 cells render the full 4. Tests: `tests/app/ambientPedestrians.test.ts` adds 4 new cases under `pedestrianCountForResidents` + reworks 2 existing anchor cases for the new tier values. Verified `npm test` (2167/2167), `npm run build`, `npm run check:dashes`. Closes the visual-density-flatness gap from F-014: each density step now looks visibly different in the drive scene. PR #N.
- 2026-05-06: REQ-077 lite landed (population-scaled ambient car count). Files: `src/app/[slug]/ambientTraffic.ts` (new `ambientCarCountForPopulation(totalPopulation)` helper + `RESIDENTS_PER_AMBIENT_CAR = 8` constant; returns 0 for empty cities, capped at `AMBIENT_CAR_COUNT = 12`), `src/app/[slug]/DriveSceneClient.tsx` (call site swaps the hard-coded count for the population-scaled call). Tests: `tests/app/ambientTraffic.test.ts` adds 6 cases under a new `ambientCarCountForPopulation` describe block. Verified `npm test` (2162/2162), `npm run build`, `npm run check:dashes`. The full per-trip-demand traffic spawn (REQ-077 spec text bullet) stays deferred; this slice ties the existing random-roam fleet to population so the visible traffic responds to the player's residential growth. PR #N.
- 2026-05-06: F-014 ambient pedestrians landed (REQ-076 spec text follow-on). PR #140. Files: `src/app/[slug]/ambientPedestrians.ts` (new pure module: `pedestrianAnchors(population, cellToWorld)` returns one anchor per populated zone cell capped at `PEDESTRIANS_PER_CELL_CAP = 4` with sorted-key iteration for replay stability and `Number.isFinite` guards; `pedestrianOffsetWithinCell(index, cellSize)` lays figures out on a small square inside the cell), `src/app/[slug]/DriveSceneClient.tsx` (mounts a small box-mesh per figure with deterministic per-mesh bob phase computed from cell + within-cell index; effect deps gain `simState.population`). Tests: `tests/app/ambientPedestrians.test.ts` ships 7 cases covering anchor emission, residents=0 skipping, count cap, sorted-key iteration, and the four-corner offset layout. Verified `npm test` (2156/2156), `npm run build`, `npm run check:dashes`, `git diff --check`, `npx playwright test e2e/sim.spec.ts e2e/drive.spec.ts --project=chromium` (30/30).
- 2026-05-06: F-017 happiness coverage perf landed. `computeCityHappiness` and `applyHappinessTick` now scope coverage computation to populated cells only via `cellCoverage(row, col, services)` from `src/lib/sim/servicesSolver.ts`. The `zones` parameter stays on both signatures, used only as a membership gate so populated-but-not-zoned cells (transient state between `applyEraseZone` and the next growth-tick population sync) contribute 0 coverage, matching the prior `solveServicesCoverage(zones, ...)[key] === undefined` semantics. Per-tick cost drops from `O(|zones| log |zones| + |zones| * |services|)` to `O(R * |services|)`. Verified via the existing 2144/2144 unit suite passing unchanged. PR #136.
- 2026-05-06: REQ-079 density decline (city-wide first cut) landed via `maybeGrowZones`. When `cityHappiness <= DECLINE_HAPPINESS_THRESHOLD = 25`, every density-greater-than-0 zoned cell steps DOWN by 1 on a growth tick (cells stay zoned at density 0 so the player can recover the layout without re-painting). Files / tests / verification covered in the parallel build-log entry under `docs/gdd/15-zoning-and-business.md` because the helper is shared. Pedestrians (REQ-076 spec text) deferred to F-014; per-cell decline + employment / power gating still a follow-on. PR #N.
- 2026-05-06: REQ-076 multi-input citizen happiness landed. Files: `src/lib/sim/state.ts` (new constants `WASTE_HAPPINESS_WEIGHT = 50`, `COVERAGE_HAPPINESS_WEIGHT = 4`, `TAX_HAPPINESS_WEIGHT = 200`, `TAX_NEUTRAL_RATE = 0.10` plus header comment block describing the four-input penalty stack), `src/lib/sim/events.ts` (`computeCityHappiness` and `applyHappinessTick` extended to take `(water, population, disasters, services, zones, taxRates)`; new formula = clamp(0..100, 100 - waste_penalty - coverage_penalty - tax_penalty - earthquake_penalty); waste reads avg of `water.wasteAccumulation` over populated cells normalized by `WASTE_MAX_PER_CELL`; coverage reads `solveServicesCoverage(zones, services)` and averages the per-populated-cell coverage count via the existing `coverageCount(coverage)` helper from `servicesSolver.ts` (range 0..5); the formula `(5 - avgCoverage) * COVERAGE_HAPPINESS_WEIGHT` caps at 20 when `avgCoverage === 0` (whether services are absent or just out of range); tax reads `max(0, taxRates.residential - TAX_NEUTRAL_RATE) * TAX_HAPPINESS_WEIGHT`; earthquakes use the existing per-active `EARTHQUAKE_HAPPINESS_PENALTY = 25`; `applyTick` threads `monsterDamaged.services`, `monsterDamaged.zones`, `state.taxRates` so the happiness reducer reads post-disaster + post-economy infra on the same tick), `src/lib/sim/servicesSolver` import for `coverageCount` + `solveServicesCoverage` added to events.ts. Tests: `tests/lib/sim/events.test.ts` two pre-existing cases updated for the new formula expected values (drained-but-uncovered residential city = 80 = 100 minus the full 20-point coverage penalty; fully-derelict-no-services-no-tax city = 30 = 100 minus 50 waste minus 20 coverage), and a new tax-penalty case asserts a drained-but-uncovered city at 14% residential tax sits at 72 (= 80 minus `(0.14 - 0.10) * 200 = 8`). Verified `npm test` (2132/2132), `npm run build`, `npm run check:dashes`, `git diff --check`, `npx playwright test e2e/sim.spec.ts --project=chromium` (24/24). Pedestrians (REQ-076 spec text bullet) deferred to F-014; NPC vehicle traffic (REQ-077), trip demand counters (REQ-078), demand-gated growth (REQ-079) all stay deferred to follow-on slices. PR #130.
- 2026-05-06: REQ-075 citizens slice 1 of N landed (population bucket + density-tied growth + editor readout). Files: `src/lib/sim/state.ts` (tightened `PopulationBucketSchema` from passthrough to strict `{ cells: Record<key, { residents, tripDemand }>, totalPopulation, totalTripDemand }`; new `RESIDENTIAL_CAPACITY_BY_DENSITY` constant Record (density 0=0, 1=4, 2=12, 3=40 residents per cell; SimCity 2000 organic-growth taxonomy scaled to v1 numbers, tunable as the citizen happiness / employment layers ship), strict `PopulationCellSchema` with `residents` and `tripDemand` fields, `EMPTY_POPULATION_BUCKET = { cells: {}, totalPopulation: 0, totalTripDemand: 0 }` frozen at module load; `EMPTY_SIM_STATE.population` now points at `EMPTY_POPULATION_BUCKET` instead of `{}`), `src/lib/sim/events.ts` (extended `applyTick` to call new exported pure helper `syncPopulationToZones(population, zones)` on every growth tick (every `GROWTH_INTERVAL_TICKS` = 20 ticks); the helper iterates zone cells, sets residents = `RESIDENTIAL_CAPACITY_BY_DENSITY[zone.density]` for residential zones, ignores commercial / industrial cells (those will contribute to job slots in REQ-083 follow-on), removes population entries for cells that fell out of the zones map (eraseZone or retype), tracks a `changed` flag so an idempotent sync returns the input bucket reference, and recomputes `totalPopulation` + `totalTripDemand` summaries for HUD readouts. Non-growth ticks skip the sync entirely so the per-tick cost stays bounded; the eventual-consistency model means an eraseZone clears the population entry within ~5s at 1x), `src/app/[slug]/edit/EditorClient.tsx` (added `editor-sim-population` readout span next to the existing tick readout in the sim-speed toolbar with `data-sim-population` attribute mirroring `simState.population.totalPopulation`). Tests: `tests/lib/sim/state.test.ts` adjusted the per-bucket-empty-object assertion to drop population (population is now strict, not passthrough) and added a "population bucket initialized to empty cells + zero totals" case; the passthrough-acceptance case swapped from population to water (water is still passthrough so the test still proves the substrate-tightening pattern). `tests/lib/sim/events.test.ts` adds 8 new cases under "population follows zone density (REQ-075 slice 1)" covering: placeZone alone keeps residents 0 (density 0 = 0 capacity), first growth tick on a residential bumps to 4, subsequent growth ticks step through 12 then 40, commercial / industrial zones do not contribute to residents, totalPopulation sums across multi-cell residential, eraseZone clears the population entry on the next growth tick, paused sim does not advance population, two-replay determinism over a mixed event log. `e2e/sim.spec.ts` adds 1 new case "residential zone growth bumps population readout" that switches to 4x speed, paints a residential cell at origin, asserts pop=0 initially, waits up to 4s for the first growth tick (1.25s wall-time at 4x = 250ms / 4 per tick * 20 ticks), asserts pop=4. Verified `npm run type-check`, `npm test` (1903/1903), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 11/11 local. Pedestrians (REQ-076), NPC vehicle traffic (REQ-077), trip demand counters (REQ-078), demand-gated growth (REQ-079) all stay deferred to follow-on slices. PR #N.
