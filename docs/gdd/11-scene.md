## 11. Scene

**Status:** partial

The three.js scene is the visual surface the drive view renders against. v1 ships a placeholder scene that swaps colored quads and extruded boxes for textured glyphs in a future polish slice; the goal of v1 is the build / drive round trip, not the visual fidelity of the city.

This file is the canonical spec for the scene requirements (REQ-044 through REQ-047).

## Lighting and ground (REQ-044)

The scene uses a noon-style lighting rig: an ambient light at 0.6 intensity keeps unlit faces from going pure black, and a directional key light at 0.9 intensity from `(60, 100, 40)` reads as the noon sun from the south-east so extruded buildings cast a believable shadowless lift. The renderer clears to a muted blue sky color (`SKY_COLOR = 0xbfd9e8`); the ground plane fills the world below `y = 0` with a warm sand / cream (`GROUND_COLOR = 0xf2ecd9`) that matches the editor's snap-grid background so the drive view feels like the same world the editor draws.

## Street pieces (REQ-045)

Street pieces render as flat colored `PlaneGeometry` quads at the cell center, lifted by `PIECE_GROUND_LIFT = 0.02` to avoid z-fighting with the ground plane and rotated around the world Y axis by the persisted rotation. v1 uses a single asphalt-grey palette per type group (cardinal basics darkest, intersections lightest) so the shape vocabulary reads from the orbit view; per-piece glyphs (curve outlines, intersection crosswalks, sweep arcs) wait for the textured visuals slice. Multi-cell footprints (REQ-059) and sampled centerlines (F-003) wait for the drive runtime port.

## Buildings (REQ-046)

Buildings render as `BoxGeometry` extrusions sized to the cell footprint with per-type heights and colors so the four placeholder primitives form a visible silhouette vocabulary from the orbit view: small house shortest, factory tallest, shop and mid-house in between with distinct hues. Boxes are anchored at the cell center on the ground plane and rotated around the world Y axis by the persisted rotation. Multi-cell building footprints (Q-004 option C) wait until playtest signal.

## Vehicle (REQ-047)

v1 ships a primitive-composed placeholder car at the spawn anchor (REQ-036) so the build / drive loop has a visible vehicle ahead of physics (REQ-031), input (REQ-034 / REQ-035), and the chase camera (REQ-033). The car is a single `THREE.Group` named `placeholder-car` containing four meshes: a body box (`CAR_LENGTH` x `CAR_WIDTH` x `CAR_BODY_HEIGHT` in `CAR_BODY_COLOR`), a smaller cabin box on top (`CAR_CABIN_LENGTH` x `CAR_CABIN_WIDTH` x `CAR_CABIN_HEIGHT` in `CAR_CABIN_COLOR`, offset toward the rear by `CAR_CABIN_OFFSET` so the windscreen line reads forward), and four wheel cylinders (`CAR_WHEEL_RADIUS`, `CAR_WHEEL_THICKNESS`, in `CAR_WHEEL_COLOR`) positioned per `carWheelOffsets()` so the front and rear axles mirror across the body's `z` axis and the left and right wheels mirror across the body's `x` axis. The group sits at the spawn anchor world position with `rotation.y` taken from the first piece's persisted rotation so the car faces the orientation the future physics integrator will inherit. Vertical placement uses `carBodyY()` and `carCabinY()` so the wheels touch the ground without z-fighting and the cabin sits on the body.

The car is a static mesh in v1: it does not move, accept input, or update across frames. The next slices attach the keyboard input (REQ-034), physics integrator (REQ-031), wheel contact (REQ-032), and chase camera (REQ-033) to the same group. The mesh is grouped under one node so a future slice can attach a transform-driven update to one node without re-walking the children. Placeholder primitives stay until a higher-fidelity model lands; the GLTF import path is deliberately not introduced in v1 to avoid pulling a loader into the bundle before the loop is fun.

## Out of scope for v1

- Per-piece textured glyphs (curve outlines, crosswalks, lane markings).
- Building decorations (windows, roofs, chimneys).
- Environment props (trees, signs, traffic lights).
- Sky dome mesh; v1 uses the renderer clear color.
- Shadow casting; v1 lighting is shadowless to keep the renderer cheap.

### Build log

- 2026-05-04: drive scene now drops one ground quad per footprint cell so multi-cell pieces (mega sweep, hairpin, future arc45 / diagonal once their palette entries land under REQ-058 / REQ-060 / REQ-059) render without visual holes (REQ-045 multi-cell). Files: `src/app/[slug]/driveScene.ts` (new pure `pieceFootprintWorldCells(piece)` helper that resolves a piece's `footprint` field, or the implicit `[{ dr: 0, dc: 0 }]` default for single-cell pieces, into world-space cell centers with `{ row, col, x, z }[]` shape; `DEFAULT_PIECE_FOOTPRINT` is duplicated locally so the drive-scene module stays free of the editor dependency tree, mirroring the streetCellSet pattern in `offStreetPenalty.ts`), `src/app/[slug]/DriveSceneClient.tsx` (street-piece render loop now iterates `pieceFootprintWorldCells(piece)` and instantiates one `THREE.Mesh` per cell, reusing the shared `PlaneGeometry` and the per-piece `MeshLambertMaterial`; persisted rotation flows through to every sub-quad so the next slice that introduces per-cell glyphs inherits the correct orientation). Tests: `tests/app/driveScene.test.ts` (7 new cases under a `pieceFootprintWorldCells (REQ-045 multi-cell ground meshes)` block: single-cell return shape, `cellToWorld` agreement on the single-cell coords, four-cell expansion with offset coverage and per-cell `cellToWorld` agreement, negative coords plus negative-offset hairpin-style expansion, fresh-array contract on each call, declared-order preservation, bounds agreement with `cityWorldBounds`). v1 has no multi-cell pieces in the editor palette today (REQ-058 / REQ-060 stay deferred under F-002) so the visible behavior is unchanged for every piece a builder can place via the editor; the slice closes the contract for any hand-edited or future-saved multi-cell piece. PR #N.

- 2026-05-04: placeholder player vehicle landed (REQ-047). Files: `src/app/[slug]/driveScene.ts` (`CAR_BODY_COLOR` / `CAR_CABIN_COLOR` / `CAR_WHEEL_COLOR` per-part colors, `CAR_LENGTH` / `CAR_WIDTH` / `CAR_BODY_HEIGHT` body footprint, `CAR_CABIN_LENGTH` / `CAR_CABIN_WIDTH` / `CAR_CABIN_HEIGHT` / `CAR_CABIN_OFFSET` cabin geometry, `CAR_WHEEL_RADIUS` / `CAR_WHEEL_THICKNESS` / `CAR_WHEEL_INSET` / `CAR_AXLE_OFFSET` / `CAR_GROUND_LIFT` wheel placement, `carWheelOffsets()` returning a fresh four-wheel array with `side` / `axle` tags, `carBodyY()` and `carCabinY()` deriving vertical positions from the same constants), `src/app/[slug]/DriveSceneClient.tsx` (renders a `THREE.Group` named `placeholder-car` at the spawn anchor when `city.pieces.length > 0` containing the body box, cabin box, and four wheel cylinders rotated 90deg around z so the round face is the contact patch; replaces the chevron spawn marker with the car composition; exposes `data-vehicle="true" | "false"` on the scene root). Tests: `tests/app/driveScene.test.ts` (added 13 cases covering the car constants, body-cabin-wheel sizing invariants, four-wheel mirror symmetry, fresh-array contract, and body / cabin vertical placement), `e2e/drive.spec.ts` (asserts `data-vehicle="false"` on both the scaffold and empty-state specs since the playwright webServer runs without KV and `loadCity` returns the empty city). REQ-047 flips `not_started` to `done`. PR #N.

- 2026-05-04: scene scaffold landed (REQ-044 ground / sky / lights, REQ-045 street pieces, REQ-046 building extrusions). Files: `src/app/[slug]/driveScene.ts` (helper module owns `SKY_COLOR`, `GROUND_COLOR`, ambient / directional intensities and position, camera defaults, `BUILDING_HEIGHTS` / `BUILDING_COLORS` / `PIECE_COLORS` maps, `cellToWorld` and `rotationToRadians` conversions, `cityWorldBounds` for camera fit and the empty-state branch), `src/app/[slug]/DriveSceneClient.tsx` (use-client three.js mount that owns the renderer / scene / camera lifecycle, lights, ground plane, per-piece quad meshes, per-building box meshes, and resize handling), `src/app/[slug]/page.tsx` (slug landing now mounts the scaffold instead of the Create CTA placeholder), `tests/app/driveScene.test.ts` (helper-module coverage for constants, color and height totality, world-coordinate conversions, bounds), `e2e/drive.spec.ts` (Playwright smoke that confirms the canvas mounts and the empty-state prompt renders for a fresh slug). PR #N.
