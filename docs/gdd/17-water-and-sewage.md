# 17. Water and Sewage

**Status:** partial

Water is the second gating utility for zone growth. Same shape as power: plants pump water, pipes transmit, cells consume. Sewage is the dual: cells produce waste, sewage pipes route to treatment, untreated waste tanks citizen happiness.

This file is the canonical spec for the water + sewage layer (REQ-090 through REQ-094).

## What this section covers

- **REQ-090** Water source placement. v1 source types: `water-tower` (small, 50 units) and `pump-station` (large, 200 units, requires placement near a water tile when terrain ships). Editor toolbar `Water` tab.
- **REQ-091** Water pipes. Single-cell pipe pieces, snap-grid, like power lines but visually distinct (blue tint).
- **REQ-092** Sewage layer. Each populated cell produces N waste per tick. Waste flows toward the nearest connected `sewage-treatment` plant via dedicated sewage-pipe cells. Untreated waste accumulates and tanks happiness.
- **REQ-093** Connectivity solver. Mirrors REQ-087 (power) shape: per-tick connected-component sum.
- **REQ-094** Drive-mode visible signal. Water towers are visible from the car (tall cylindrical primitives). Sewage plants are large and unmistakable. Underground pipes are NOT visible in drive mode (placement-only signal).

## Schema sketch

```ts
City.sim.water = {
  sources: Array<{ id: string; kind: 'water-tower' | 'pump-station'; row: number; col: number; capacityUnits: number }>
  pipes: Array<{ row: number; col: number; kind: 'water' | 'sewage' }>
  treatmentPlants: Array<{ id: string; row: number; col: number; capacityUnits: number }>
  cellWaterStatus: Record<string, 'served' | 'unserved'>
  cellWasteAccumulation: Record<string, number>
}
```

## Out of scope for this section

- Terrain water (rivers, lakes). v1 is flat ground; pump-stations work anywhere until terrain ships.
- Per-pipe diameter, flow rate physics. Treat as connected-component capacity sum.

### Build log

- 2026-05-06: REQ-092 sewage slice 4 of N landed (per-tick waste accumulation reducer; happiness penalty defers to slice 5). Files: `src/lib/sim/state.ts` (new `WASTE_INCREMENT_PER_TICK = 1` and `WASTE_MAX_PER_CELL = 100` tunables; `WaterBucketSchema` extended with `wasteAccumulation: Record<string, number>` (z.number().min(0)); `EMPTY_WATER_BUCKET` seeds it `{}`), `src/lib/sim/events.ts` (new `applyWasteTick(water, population, zones)` reducer that calls `solveSewageStatus`, walks every populated cell (residents > 0), resets waste to 0 for 'drained' status, increments by 1 capped at 100 for 'overloaded' / 'unmanaged', drops entries for cells no longer populated, identity-on-no-change short-circuit; wired into `applyTick` after the population sync + economy phase so the same tick that grew a zone also bumps its first waste). Type-check fallout from tightening the bucket: SnapGridView water-fallback + waterSolver.test bucketWith helper + sewageSolver.test sewageWith helper all seed `wasteAccumulation: {}`. Tests: `tests/lib/sim/state.test.ts` updates the empty-water-bucket shape assertion to include the new field. `tests/lib/sim/events.test.ts` adds 6 cases under "per-tick waste accumulation (REQ-092 sewage slice 4)" covering: empty before any populated cell, increments each tick on unmanaged cell (1 then 4), resets to 0 when drained by an adjacent treatment plant, caps at 100 after long unmanaged accumulation, drops entries when a cell is erased and population resyncs, two-replay determinism over a mixed event log. Verified `npm run type-check`, `npm test` (1999/1999), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 18/18 local. Happiness penalty wiring (citizen happiness drops as average city waste exceeds a threshold; tanks zone growth) lands in slice 5 alongside the broader citizen-happiness reducer (REQ-076 follow-on). Drive-visible water towers + sewage plants (REQ-094) land in slice 6. PR #N.
- 2026-05-06: REQ-092 sewage slice 3 of N landed (sewage connectivity solver + visible drained / overloaded / unmanaged on zones; mirrors `waterSolver.ts` pattern). Files: `src/lib/sim/sewageSolver.ts` (new pure module exporting `SEWAGE_DEMAND_PER_CELL = 1`, `CellSewageStatus = 'drained' | 'overloaded' | 'unmanaged'`, `SewageComponent` interface (transmission Set + plants + capacity), `computeSewageComponents(water)` running BFS over (treatment plants ∪ sewage pipes) with 4-direction adjacency, `solveSewageStatus(zones, water)` per-cell classifier with capacity tracked per component for stable replay, `cellSewageStatusFor(row, col, zones, water)` single-cell convenience). Water pipes are NOT in the drain transmission graph: only treatment plants + sewage pipes form the drain graph (the load-bearing distinction from the fresh-water solver). A zone adjacent to plantless sewage pipes resolves to `unmanaged` because pipes alone provide no treatment capacity. Per-cell waste demand is uniform 1 unit so a 100-cap treatment plant drains up to 100 cells. `src/app/[slug]/edit/SnapGridView.tsx` calls `solveSewageStatus` alongside the existing `solvePowerStatus`, `solveServicesCoverage`, and `solveWaterStatus`, mirrors the result onto each zone overlay's `data-zone-sewage-status` attribute. Tests: `tests/lib/sim/sewageSolver.test.ts` ships 19 cases covering `SEWAGE_DEMAND_PER_CELL`, `computeSewageComponents` empty / lone plant / lone sewage pipe / plant-with-adjacent-pipe / water-pipes-not-in-drain-graph / sewage chain / no-diagonal / two-replay determinism, `solveSewageStatus` empty zones / no sewage / plant-adjacent-drained / pipe-chain-drained / water-pipe-adjacent-unmanaged / plantless-pipes-unmanaged / capacity-cap with overloaded-overflow at 100 drained + 1 overloaded / two-replay determinism, `cellSewageStatusFor` unzoned and zoned-drained. `e2e/sim.spec.ts` adds 1 new case "zone sewage status flips to drained when wired to a treatment plant" that paints residential at (0,2), asserts unmanaged, places a treatment plant at (0,0) and a sewage pipe at (0,1), asserts drained. Verified `npm run type-check`, `npm test` (1993/1993), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 18/18 local. Per-tick waste accumulation on populated cells (cell waste counter that drops when adjacent to a non-overloaded sewage component) lands in slice 4; happiness penalty wiring lands in slice 5. PR #N.
- 2026-05-06: REQ-092 sewage slice 2 of N landed (editor Water palette gets a `source-sewage-treatment` tool + plant overlay; sewage connectivity solver + waste accumulation defer to slices 3/4). Files: `src/app/[slug]/edit/editorState.ts` (extended `WaterPaletteToolType` with `'source-sewage-treatment'`; appended `{ type: 'source-sewage-treatment', label: 'Sewage Plant' }` to `WATER_PALETTE`), `src/app/[slug]/edit/EditorClient.tsx` (imports `PlaceSewageTreatmentPlantEvent`; the water-category click handler grew a third source branch that dispatches `placeSewageTreatmentPlant` for the sewage-treatment tool; the palette button color resolver gained a 5th branch (deep brown `#4a3522` to read as the drain-side counterpart to the water tower)), `src/app/[slug]/edit/SnapGridView.tsx` (new `SEWAGE_TREATMENT_FILL` / `SEWAGE_TREATMENT_STROKE` constants; renders each `water.treatmentPlants` entry as a 4px-inset cell-sized rect with `editor-sewage-treatment-plant` testid + `data-sewage-plant-row` / `data-sewage-plant-col` attributes between the water-source layer and the power-plant layer so the visual stacking matches the schema's water-bucket grouping). Tests: `e2e/sim.spec.ts` adds 1 new case "Water palette includes a sewage-treatment tool that paints a plant overlay" that switches to Water category, asserts the new tool button is visible, selects it, paints at (2, 3), asserts the plant overlay renders. Verified `npm run type-check`, `npm test` (1974/1974), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 17/17 local. Plant erase ships alongside `eraseWaterSource` in the same future slice (currently the editor cannot erase plants OR water sources, mirroring REQ-090 slice 1's deferral). Sewage connectivity solver mirroring `waterSolver.ts` (BFS over treatment plants + sewage pipes) lands in slice 3; per-tick waste accumulation on populated cells with drain-when-connected lands in slice 4. PR #N.
- 2026-05-06: REQ-092 sewage slice 1 of N landed (schema + place/erase events + reducer for sewage treatment plants; UI palette + connectivity solver + waste accumulation defer to slices 2/3/4). Files: `src/lib/sim/state.ts` (new `SEWAGE_TREATMENT_CAPACITY = 100` waste-units drained per tick per plant, `SEWAGE_TREATMENT_MAINTENANCE_PER_TICK = 0.6` for the economy reducer to pull in a future slice, strict `SewageTreatmentPlantSchema` carrying `{ row, col }` (no per-plant tier in v1; capacity is the single uniform constant), extended `WaterBucketSchema` with `treatmentPlants: SewageTreatmentPlant[]`, extended `EMPTY_WATER_BUCKET` to seed the array empty), `src/lib/sim/events.ts` (2 new strict event schemas: `PlaceSewageTreatmentPlantEventSchema` carrying row/col, `EraseSewageTreatmentPlantEventSchema` carrying row/col; both routed in the `SimEventSchema` discriminated union; new reducer cases `applyPlaceSewageTreatmentPlant` idempotent on duplicate anchor, `applyEraseSewageTreatmentPlant` removes by anchor and identity on missing). Two type-check fallouts from tightening the bucket: `src/app/[slug]/edit/SnapGridView.tsx` water-fallback object now includes `treatmentPlants: []`, and `tests/lib/sim/waterSolver.test.ts` `bucketWith` helper now seeds `treatmentPlants: []`. Tests: `tests/lib/sim/state.test.ts` updates the empty-water-bucket shape assertion to include the new field. `tests/lib/sim/events.test.ts` adds 5 cases under "sewage treatment plant events (REQ-092 sewage slice 1)" covering: appends a single plant, identity on duplicate anchor, erase removes a placed plant, erase identity on missing, two-replay determinism over a mixed plant event log. Verified `npm run type-check`, `npm test` (1974/1974), `npm run build`, `npm run check:dashes`, `git diff --check` all green. Sewage UI tool in the Water palette + connectivity solver mirroring `waterSolver.ts` (sewage variant: BFS over (treatment plants + sewage pipes), per-cell drain status) lands in slice 2; per-tick waste accumulation on populated cells, drained when connected, lands in slice 3; happiness penalty wiring lands in slice 4. PR #N.
- 2026-05-06: REQ-093 water connectivity solver landed (slice 3 of N for the water layer; mirrors REQ-087 power solver pattern). Files: `src/lib/sim/waterSolver.ts` (new pure module exporting `WATER_DEMAND_PER_CELL = 1`, `CellWaterStatus = 'served' | 'brownout' | 'unserved'`, `WaterComponent` interface carrying transmission Set + sources + capacity, `computeWaterComponents(water)` running BFS over (source anchors ∪ water-pipe cells) with 4-direction orthogonal adjacency, `solveWaterStatus(zones, water)` per-cell classifier with capacity tracked per component for stable replay, `cellWaterStatusFor(row, col, zones, water)` single-cell convenience). Sewage pipes are NOT part of the supply transmission graph: only sources + water pipes form the supply graph; sewage pipes scope to their own connectivity walk in the future REQ-092 sewage-flow slice. Per-cell demand is 1 unit so a 50-cap water-tower serves up to 50 cells; a 200-cap pump-station up to 200. `src/app/[slug]/edit/SnapGridView.tsx` calls `solveWaterStatus` alongside the existing `solvePowerStatus` and `solveServicesCoverage`, mirrors the result onto each zone overlay's `data-zone-water-status` attribute (served / brownout / unserved). Tests: `tests/lib/sim/waterSolver.test.ts` ships 18 cases covering `WATER_DEMAND_PER_CELL` constant, `computeWaterComponents` empty bucket / lone tower / lone pipe / source-with-adjacent-pipe / sewage-not-in-supply-graph (the load-bearing distinction from power) / pipe chain / no-diagonal-connectivity / two-replay determinism, `solveWaterStatus` empty zones / no water / source-adjacent-served / pipe-chain-served / sewage-adjacent-unserved / capacity-cap with brownout-overflow at 50 served + 5 brownout / two-replay determinism, `cellWaterStatusFor` unzoned and zoned-served. `e2e/sim.spec.ts` adds 1 new case "zone water status flips to served when wired to a water source" that paints residential at (0,2), asserts unserved, places a water-tower at (0,0) and a water pipe at (0,1), asserts served. Verified `npm run type-check`, `npm test` (1969/1969), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 16/16 local. Sewage waste accumulation (REQ-092) lands in slice 4: each populated cell produces N waste per tick that flows through sewage pipes to treatment plants; untreated waste tanks happiness. Drive-visible water towers + sewage plants (REQ-094) land in slice 5. PR #N.
- 2026-05-06: REQ-090 water slice 2 of N landed (editor Water tab + 4 tools). Files: `src/app/[slug]/edit/editorState.ts` (extended `PaletteCategory` to include `'water'`; new `WaterPaletteToolType` union (4 entries: `source-water-tower` / `source-pump-station` / `pipe-water` / `pipe-sewage`), `WATER_PALETTE` constant, `DEFAULT_WATER_TOOL = 'pipe-water'` because pipe edits dominate water-grid placement), `src/app/[slug]/edit/EditorClient.tsx` (added `selectedWaterTool` state, extended the tablist + label-resolver to include 'water' (Water label), added a per-category palette branch rendering 4 water buttons with kind-distinct palette colors (water-tower light blue, pump-station deep blue, water pipe bright blue, sewage pipe brown), extended `handleCellClick` to dispatch `EraseWaterPipeEvent` (erase mode) or branch on the selected tool: source tools dispatch `PlaceWaterSourceEvent`, pipe tools dispatch `RunWaterPipeEvent` with the kind discriminator, passed `simState.water` to the SnapGrid as a new optional prop), `src/app/[slug]/edit/SnapGridView.tsx` (added optional `water?: WaterBucket | null` prop; new `WATER_SOURCE_FILL` / `WATER_SOURCE_STROKE` and `WATER_PIPE_FILL` / `WATER_PIPE_STROKE` constant Records; renders each pipe cell as a 50%-cell-size kind-tinted square overlay (water = bright blue, sewage = brown) with `editor-water-pipe` testid plus `data-water-pipe-row` / `data-water-pipe-col` / `data-water-pipe-kind` attributes; renders each source as an `editor-water-source` overlay 4px-inset from cell with kind-distinct fill/stroke + `data-water-source-kind` attribute; both layers render after the services layer but before the connector glyphs so the place / erase ghosts and open-end warnings stay legible). Tests: `e2e/sim.spec.ts` adds 1 new case "Water tab exposes 4 tools and paints a water tower + pipe" that switches to Water category, asserts all 4 tools visible, asserts pipe-water is the default selected, switches to source-water-tower and paints at (0,0) (source overlay appears), switches to pipe-water and paints at (0,1) (water pipe overlay appears with kind=water), switches to pipe-sewage and paints at (1,0) (sewage pipe overlay appears with kind=sewage). Verified `npm run type-check`, `npm test` (1951/1951), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 15/15 local. Connectivity solver mirroring `powerSolver.ts` pattern (REQ-093) lands in slice 3, sewage waste accumulation (REQ-092) in slice 4, drive-visible water towers + sewage plants (REQ-094) in slice 5. PR #N.
- 2026-05-06: REQ-090 water slice 1 of N landed (schema + place/erase events + reducer; UI + connectivity solver defer to slices 2/3). Files: `src/lib/sim/state.ts` (new `WaterSourceKindSchema` (water-tower / pump-station), `WATER_SOURCE_CAPACITY` per-kind (water-tower 50, pump-station 200), `WATER_PIPE_MAINTENANCE_PER_TICK = 0.04` and `WATER_SOURCE_MAINTENANCE_PER_TICK` per-kind (water-tower 0.3, pump-station 0.8) for the economy reducer to pull in slice 2, `WaterPipeKindSchema` (water / sewage), strict `WaterSourceSchema` and `WaterBucketSchema` (`{ sources: WaterSource[], pipes: Record<key, WaterPipeKind> }`), `EMPTY_WATER_BUCKET` frozen, `waterPipeKey` matching the existing convention; `EMPTY_SIM_STATE.water` now points at `EMPTY_WATER_BUCKET` instead of `{}`), `src/lib/sim/events.ts` (3 new strict event schemas: `PlaceWaterSourceEventSchema` carrying kind/row/col, `RunWaterPipeEventSchema` carrying kind/row/col, `EraseWaterPipeEventSchema` carrying row/col; all routed in the `SimEventSchema` discriminated union; the `PlaceholderLayerEventSchema` enum drops `placeWaterSource` since the typed variants now route it; only `spawnDisaster` (REQ-105) remains as a forward-compat placeholder. New reducer cases: `applyPlaceWaterSource` idempotent on duplicate anchor+kind allowing different kinds at the same anchor for v1, `applyRunWaterPipe` returns identity on duplicate-same-kind and overwrites kind on different-kind-same-cell so a player retyping water -> sewage at one cell flips cleanly, `applyEraseWaterPipe` removes any pipe (water or sewage) at the cell). Tests: `tests/lib/sim/state.test.ts` adds 1 case for the strict `EMPTY_SIM_STATE.water` shape (`{ sources: [], pipes: {} }`); the passthrough-acceptance case swapped from water to disasters (disasters is the only remaining passthrough bucket). `tests/lib/sim/events.test.ts` adds 9 cases under "water layer events (REQ-090 slice 1)" covering: appends a single water tower, identity on duplicate source anchor+kind, runs a water pipe, runs a sewage pipe, identity on duplicate same-kind, overwrites kind on different-kind-same-cell, eraseWaterPipe removes a placed pipe, eraseWaterPipe identity on missing, two-replay determinism over a mixed water event log. Verified `npm run type-check`, `npm test` (1951/1951), `npm run build`, `npm run check:dashes`, `git diff --check` all green. UI tab + 4 tools (water-tower, pump-station, water pipe, sewage pipe) land in slice 2, connectivity solver mirroring `powerSolver.ts` pattern lands in slice 3, drive-visible water towers + sewage plants + pollution-from-coal land in subsequent slices. PR #N.
