# 21. Sim-as-primary View

**Status:** partial

The sim-as-primary view is the top-down or 2.5D-isometric surface that replaces the existing snap-grid editor as the default `/<slug>` route. The drive view becomes a toggle from this surface, not the default. This is the UI side of the Q-009 pivot.

This file is the canonical spec for the sim-as-primary view (REQ-110 through REQ-114).

## What this section covers

- **REQ-110** Sim view as default route. `/<slug>` renders the sim view. The existing drive view moves to `/<slug>/drive`. The existing editor at `/<slug>/edit` is absorbed into the sim view's toolbar; placing zones / infrastructure happens directly on the sim canvas. Old routes either redirect or stay supported during a migration window.
- **REQ-111** Top-down camera. Default angle is overhead 45-degree iso (SimCity 4 standard). Camera supports pan, rotate (90deg snaps), zoom. Mouse-wheel zoom; click-drag pan; Q/E rotate.
- **REQ-112** Toolbar redesign. Tabs for zones, infrastructure (power, water, sewage), services, transit (streets), terrain (future). The current Streets / Buildings tab pair extends; nothing is removed.
- **REQ-113** Sim controls panel. Pause / 1x / 2x / 4x speed selector. Tax sliders. Treasury readout. Demand bars. Always visible in sim view.
- **REQ-114** Drive toggle. A persistent button labeled `Drive` in the sim view drops the player to the existing drive scene at the camera's current focus point. Press `Esc` or click `Sim view` to return. The sim continues running while the player is in the car.

## Migration notes

The existing `/<slug>` and `/<slug>/edit` routes need careful handling. Recommended:

1. Ship the new sim view at `/<slug>/sim` first. Existing routes unchanged.
2. Once the sim view feels right, swap the defaults: `/<slug>` becomes the sim view, `/<slug>/edit` redirects to `/<slug>/sim`, drive moves to `/<slug>/drive`.
3. Keep the old route shapes alive long enough that any external links stay working. Add 301 redirects for the inverted paths.

## Out of scope for this section

- VR / 3D city view (defer until everything else feels right).
- Mobile-touch pinch-zoom and rotate gestures (touch-controls.ts inheritance covers basic pan; full gesture support is its own slice).
- A minimap separate from the existing drive minimap (the sim view IS the minimap; no separate widget).

### Build log

- 2026-05-18: REQ-110 polish: rename the editor's "Streets" toolbar tab to "Transit" to align with the GDD taxonomy (`zones, infrastructure, services, transit, terrain`). Label-only change: the internal `PaletteCategory = 'street'` enum value, the `editor-palette-category-street` testid, and dispatched event kinds all stay the same so the cross-file ripple stays out of the slice. `src/app/[slug]/edit/EditorClient.tsx` label map + welcome hint copy updated; `src/app/[slug]/page.tsx` blurb updated. One new e2e assertion locks the rename (`expect(streetTab).toHaveText('Transit')`). PR #254.

- 2026-05-18: REQ-110 polish: drive HUD's Edit CTAs intercept plain left-clicks and navigate to `/<slug>?focus=row,col` with the car's current cell, closing the bidirectional editor / drive camera handoff started in PR #251 (editor -> drive `?spawn=`) and PR #252 (editor `?focus=` reader). `DriveSceneClient.tsx` adds a `carCellRef` updated each frame inside the imperative vehicle-attrs loop and a shared `handleEditClick` callback that uses `next/navigation`'s `useRouter` to push the focus URL via `focusOverrideQuery`. Both `drive-edit-cta` and `drive-pause-edit-cta` Links wire `onClick={handleEditClick}` while keeping a bare `href` so modifier-clicks and middle-clicks open the bare editor in a new tab. New e2e in `e2e/drive.spec.ts` reads `data-car-x` / `data-car-z`, computes the expected cell, clicks the pause-menu Edit CTA, and asserts the resulting URL. PR #253.

- 2026-05-18: REQ-110 polish: editor reads `?focus=row,col` deep-link and seeds the initial viewport so the camera lands on a known cell. New pure helper `viewportPanToCell(cell, zoom?)` in `src/app/[slug]/edit/gridViewport.ts` is the inverse of `viewportFocusCell`. New `src/app/[slug]/edit/focusOverride.ts` ships `parseFocusOverride` (re-exports `parseSpawnOverride`), `focusOverrideQuery`, and `readFocusSearchParam`. `src/app/[slug]/page.tsx` extends searchParams to include `focus?: string | string[]` and passes the parsed cell as `initialFocus` to `EditorClient`. `EditorClient.tsx` accepts the prop and lazy-inits its viewport via `viewportPanToCell(initialFocus)` when provided. Tests: 5 new cases in `tests/app/gridViewport.test.ts` covering `viewportPanToCell` (identity at origin, single-cell pan, viewportFocusCell round-trip, non-default zoom round-trip, far-cell clamp); new `tests/app/focusOverride.test.ts` (9 cases). E2e: 1 new spec in `e2e/editor.spec.ts` covers default, `?focus=0,3` pans east 3 cells, malformed value falls through silently. PR #252.

- 2026-05-18: REQ-110 polish: editor Drive CTA encodes the viewport's focus cell into a `?spawn=row,col` URL so driving from the editor lands the car under the cell the camera is looking at. New pure `viewportFocusCell(viewport)` in `src/app/[slug]/edit/gridViewport.ts` inverts the viewBox center pixel into a `(row, col)` cell using the existing `CELL_PIXELS` / `GRID_RADIUS` constants. `src/app/[slug]/edit/EditorClient.tsx` memoizes `driveSpawnQuery = spawnOverrideQuery(resolveSpawnOverride(viewportFocusCell(viewport), city.pieces))` and appends it to the Drive CTA href; the resolve step silently drops the override when the focus cell is empty so an empty-corner viewport still produces a clean bare-href. New `data-spawn-query` data attribute mirrors the appended query for e2e assertions. Tests: 4 new cases in `tests/app/gridViewport.test.ts` (default focus on origin, one-cell pan shifts, zoom-2 focus tracking, integer-only output); 1 new spec in `e2e/editor.spec.ts` confirms the dynamic href update after placing a piece at (0,0). PR #251.

- 2026-05-10: Scaffold polish: stamped `data-view="sim"` / `data-route="sim"` on the canonical sim view `<main>` in `src/app/[slug]/page.tsx`. The route-level identity marker gives e2e (and the REQ-114 sim / drive toggle, when it lands) a stable hook to assert the bare-slug surface IS the sim view, independent of which client component renders below. Two new cases in `e2e/sim.spec.ts`: one asserts the marker on the bare-slug landing, one asserts the legacy `/<slug>/sim` redirect lands on a page carrying the marker. The REQ-110 scaffold scope from the original dot (top-down iso camera, pan / rotate / zoom, basic toolbar with zone tab) had already shipped across slices A through D (PRs #166, #167, #168, #169, #170); this slice closes the dot by giving the sim-view identity a stable name in the DOM. REQ-110 stays `partial` because REQ-114 (Drive toggle at camera focus) and the REQ-112 tab rename pass (Streets to Transit, add Infrastructure / Terrain) remain. PR #213.

- 2026-05-08: Cleanup R1: hoisted iso projection + rotation helpers out of `src/app/[slug]/edit/` into a new `src/lib/render/iso/` package (`projection.ts`, `rotation.ts`, `index.ts` barrel) so future games can import without depending on the city app tree. Tests followed: `tests/lib/render/iso/projection.test.ts` + `tests/lib/render/iso/rotation.test.ts`. Consumer imports in `SnapGridView.tsx` and `EditorClient.tsx` flipped to `@/lib/render/iso`. Lib docstrings rewritten to describe the generic API (no editor-specific references); editor files keep their own use docs. No behavior changes; full suite green (2325 / 2325). PR #171.

- 2026-05-08: REQ-111 polish: iso camera rotate buttons. Two toolbar buttons (`↺` / `↻`) in `src/app/[slug]/edit/EditorClient.tsx` next to Reset View call the existing `handleRotateIsoCcw` / `handleRotateIsoCw` handlers from slice C, surfacing the iso rotation feature for mouse users. Buttons carry `data-testid="editor-rotate-iso-ccw"` / `editor-rotate-iso-cw`, `data-iso-rotation-deg` mirroring the live state, and aria-labels / tooltips that mention the Q and `]` keyboard shortcuts. New `e2e/sim.spec.ts` test asserts both buttons are visible by default and clicking them flips `data-viewport-rotation` through the four cardinal snaps. PR #170.

- 2026-05-08: Slice D landed: REQ-113 sim controls panel contract verified. The Pause / 1x / 2x / 4x speed selector, treasury readout, R / C / I demand bars, and R / C / I tax sliders were already implemented inside `src/app/[slug]/edit/EditorClient.tsx` (the editor that now serves as the sim-as-primary view at `/<slug>` after slice B). Slice D adds a single REQ-113-shaped e2e to `e2e/sim.spec.ts` that loads the canonical `/<slug>` route and asserts all four pieces are visible together by default, locking the always-visible contract for future slices. No code changes to `EditorClient.tsx` (existing implementation already covers the spec). Verified `npx playwright test e2e/sim.spec.ts -g "REQ-113 controls panel"` (1/1 pass), `npm run check:dashes`. PR #169.

- 2026-05-08: Slice C landed: iso camera rotate (REQ-110, REQ-111). New `src/app/[slug]/edit/isoRotation.ts` exports `ISO_ROTATION_STEP_DEG = 90`, `DEFAULT_ISO_ROTATION_DEG = 0`, `normalizeIsoRotation` (positive-modulo wrap with non-finite fallback), `rotateIsoCcw`, `rotateIsoCw`. `src/app/[slug]/edit/isoProjection.ts` `isoTransformCss(mode, rotationDeg = 0)` sums camera rotation into the iso rotate angle. `src/app/[slug]/edit/SnapGridView.tsx` accepts `viewRotationDeg?: number` (default 0), passes it to `isoTransformCss`, and mirrors `data-viewport-rotation` on the SVG root. `src/app/[slug]/edit/EditorClient.tsx` holds `isoRotationDeg` state, threads it into `<SnapGrid />`, and binds Q (ccw) and `]` (cw) in the existing keyboard handler. `]` substitutes for the canonical SimCity E binding because REQ-022 already owns E for the erase tool; documented inline so a future polish slice can introduce an alternate cw key without conflict. Tests: 12 new cases in `tests/app/isoRotation.test.ts` covering constants, positive-modulo wrap, non-finite fallback, ccw/cw stepping, the 4-rotation cycle, and the inverse property; 2 new cases in `tests/app/isoProjection.test.ts` covering `rotationDeg` summation in iso mode and its no-op behavior in flat mode. Verified `npx vitest run` (2325/2325), `npx tsc --noEmit`, dash check clean. REQ-111 status flips `not_started` -> `partial`. Slice D (sim controls panel polish) stays deferred. PR #168.

- 2026-05-08: Slice B landed: route swap (REQ-110). `/<slug>` now renders the editor / sim view (was the drive scene); the drive scene moved to `/<slug>/drive`. `/<slug>/edit` 308-redirects to `/<slug>` preserving the optional `?v=<hash>` query string. `/<slug>/sim` redirect target updated from `/<slug>/edit` to `/<slug>` so the legacy chain collapses to one hop. `buildShareUrl` now returns `/<slug>/drive`; `buildEditUrl` returns `/<slug>`. Internal links (editor Drive CTA, drive HUD Edit CTAs, home page Create CTA) all updated to the new contract. Tests: `tests/app/shareUrl.test.ts` URL assertions retuned, `tests/app/driveRoute.test.ts` / `tests/app/editRoute.test.ts` import paths flipped, `e2e/drive.spec.ts` + `e2e/editor.spec.ts` + `e2e/sim.spec.ts` + `e2e/home.spec.ts` URL assertions retuned. Verified `npm test` (2311/2311), `npm run type-check`, `npm run build`, `npm run check:dashes`. Slice C (iso camera rotate + Q/E keys) and slice D (sim controls panel polish) stay deferred. PR #167.

- 2026-05-08: Slice A landed: 45deg iso projection on the editor SnapGrid (REQ-111 visual). New `src/app/[slug]/edit/isoProjection.ts` exports `ISO_ROTATE_DEG = -45`, `ISO_SCALE_Y = 0.5`, `isoTransformCss(mode)`, and `SnapGridViewMode` union. `src/app/[slug]/edit/SnapGridView.tsx` accepts `viewMode` prop (default `'iso'`) and applies the CSS transform to the SVG element with `transform-origin: center center`; the SVG's internal coords are unchanged so existing pointer events and viewBox pan/zoom keep working. Tests: `tests/app/isoProjection.test.ts` (8 cases on the constants and the transform string); `e2e/editor.spec.ts` adds a `data-view-mode="iso"` assertion. REQ-110 status flips `not_started` -> `partial`. Slice B (route swap), slice C (iso camera rotate + Q/E keys), and slice D (sim controls panel polish) stay deferred. PR #166.
