# 20. Disasters

**Status:** done

Disasters are reactive sim events that destroy infrastructure and damage citizen happiness. Players can ignore them in fair-weather mode; the loop is more interesting if they happen.

This file is the canonical spec for the disasters layer (REQ-105 through REQ-109).

## What this section covers

- **REQ-105** Disaster types. Five v1 disaster kinds: `fire`, `flood`, `tornado`, `earthquake`, `monster`. Each has a spawn-probability function and a damage profile.
- **REQ-106** Spawn triggers. Fire spawns proportional to industrial density and missing fire coverage. Flood is wall-clock seasonal (in-game spring). Tornado / earthquake / monster are pseudo-random with low base rate; a settings toggle disables all disaster spawning ("fair-weather mode").
- **REQ-107** Damage profile. Each disaster has a footprint (single-cell for fire spawn, multi-cell for tornado swath). Damaged cells lose population and infrastructure connectivity. Service stations within range respond (fire to fire, hospital to monster damage, etc.).
- **REQ-108** Recovery. Damaged cells decay-rebuild over N ticks if services are present. Without services, the cells stay rubble.
- **REQ-109** Drive-mode visible signal. Active disasters are visually unmistakable from the car. Fire shows flame particles; flood shows water level; tornado shows rotating debris column.

## Schema sketch

```ts
City.sim.disasters = {
  active: Array<{ id: string; kind: DisasterKind; cells: Array<{ row: number; col: number }>; spawnedAtTick: number }>
  fairWeatherMode: boolean
  cellDamage: Record<string, number>  // 0 to 1, decays back to 0 with services
}
```

## Out of scope for this section

- Player-triggered disasters (the SimCity Disasters menu). v1 is all autonomous-spawn; a debug menu can land later.
- Disaster save-state per-tick replay. The active list is the truth; partial damage is the mutation log.

### Build log

- 2026-05-07: REQ-105 mass-appeal slice 2: random earthquake auto-spawn. Files: `src/lib/sim/state.ts` (new `EARTHQUAKE_AUTO_SPAWN_PROBABILITY_PER_TICK = 0.002`), `src/lib/sim/earthquakeAutoSpawn.ts` (new pure module: `earthquakeAutoSpawnHash` + `earthquakeCellPickHash` with mixing constants distinct from the fire family; `computeEarthquakeAutoSpawn(zones, disasters, tick)` returns at most one earthquake per tick, skipping if one is already active or no zones exist), `src/lib/sim/events.ts` (`applyTick` calls the new helper after fire auto-spawn and appends to the same bundle). Tests: `tests/lib/sim/earthquakeAutoSpawn.test.ts` 10 new cases. Verified `npm test` (2183/2183), `npm run type-check`, `npm run build`, `npm run check:dashes`, `npx playwright test e2e/sim.spec.ts --project=chromium` (29/29). Adds replay variability (gameplay analysis 2026-05-07): two replays of distinct event logs see different earthquake spawn moments. PR #151.
- 2026-05-06: REQ-105 + REQ-100 per-cell fire-risk overlay landed. Files: `src/app/[slug]/edit/SnapGridView.tsx` (zone-overlay loop derives `isFireRisk` from `zone.kind === 'industrial' && zone.density > 0 && !coverage?.['fire-station']`, surfaces it via `data-zone-fire-risk={true|false}`, and overrides stroke + strokeWidth to the warning red palette when true). Tests: `e2e/sim.spec.ts` adds 1 new "industrial cell carries data-zone-fire-risk='true' when uncovered, 'false' once a fire-station is in range" case. Verified `npm test` (2163/2163), `npm run build`, `npm run check:dashes`, `npx playwright test e2e/sim.spec.ts --project=chromium --grep fire-risk` (2/2). Pairs with the HUD fire-risk count from the prior slice: the player now sees both the count AND which cells contribute to it. PR #N.
- 2026-05-06: REQ-105 + REQ-100 fire-risk HUD readout landed. Files: `src/lib/sim/fireAutoSpawn.ts` (new exported `countUncoveredIndustrial(zones, services)` pure helper that walks `zones.cells` and returns the count of industrial-and-density-greater-than-0 cells not within fire-station Manhattan radius; reuses the existing `isCoveredByFireStation` logic), `src/app/[slug]/edit/EditorClient.tsx` (new inline span with `data-testid="editor-sim-fire-risk"` and `data-sim-fire-risk={count}`, mounts only when the count is greater than 0, color matches the low-happiness red so visual language stays consistent). Tests: `tests/lib/sim/fireAutoSpawn.test.ts` adds 3 new cases under a `countUncoveredIndustrial` describe block. `e2e/sim.spec.ts` adds 1 new "fire-risk readout surfaces uncovered industrial cells and clears when a fire-station is placed" case. Verified `npm test` (2148/2148), `npm run build`, `npm run check:dashes`, `git diff --check`, `npx playwright test e2e/sim.spec.ts --project=chromium` (26/26). Closes the visibility gap from the previous auto-spawn slices: the player can see the gameplay lever directly instead of inferring it from disaster spawns. PR #N.
- 2026-05-06: REQ-105 + REQ-100 density-modulated fire auto-spawn landed. Files: `src/lib/sim/fireAutoSpawn.ts` (the per-cell probability gate now multiplies `FIRE_AUTO_SPAWN_PROBABILITY_PER_TICK` by `cell.density` so density-3 industrial is 3x more fire-prone than density-1, density-2 is 2x; density-0 cells stay skipped via the existing density check). Tests: `tests/lib/sim/fireAutoSpawn.test.ts` adds 1 new "density-3 ignites at a tick where density-1 would not (density modulation)" case that finds a tick whose hash falls in the `[P, 3*P)` band and asserts density-3 ignites while density-1 at the same tick does not. The existing 8 cases all use density-1 cells where the density-modulated threshold equals the base, so they continue to pass unchanged. Verified `npm test` (2145/2145), `npm run build`, `npm run check:dashes`, `git diff --check`, `npx playwright test e2e/sim.spec.ts --project=chromium` (25/25). Player mental model: twice as dense = twice as risky; the player's gameplay lever (place fire stations) scales linearly with the threat. PR #N.
- 2026-05-06: REQ-105 + REQ-100 follow-on landed (deterministic fire auto-spawn at uncovered industrial cells). Files: `src/lib/sim/state.ts` (new `FIRE_AUTO_SPAWN_PROBABILITY_PER_TICK = 0.004` constant with header comment block tying the calibration to expected ~250 ticks (~62 wall-seconds at 1x) for a single uncovered industrial cell, ~6 wall-seconds for a 10-cell belt), `src/lib/sim/fireAutoSpawn.ts` (new pure module: `fireAutoSpawnHash(tick, row, col)` 32-bit mixing function with mixing constants `0x27d4eb2f / 0x165667b1 / 0xd3a2646c / 0xfd7046c5` distinct from the existing fire spread / fire damage hashes so the rolls are independent; `computeFireAutoSpawn(zones, services, disasters, tick)` walks every industrial zone cell with density > 0 in sorted-key order for replay stability, validates `Number.isFinite` on parsed row / col, skips cells covered by a fire-station via Manhattan radius `SERVICE_COVERAGE_CELLS['fire-station']`, skips cells with an existing fire at the same anchor, and ignites cells whose roll falls below the probability constant), `src/lib/sim/events.ts` (`applyTick` now decrements + spreads disasters first, then runs `computeFireAutoSpawn` against the post-tornado / post-monster zones and services; auto-spawned fires append to the active list so they burn their full duration before the next decrement). Tests: `tests/lib/sim/fireAutoSpawn.test.ts` ships 8 cases: uncovered industrial ignites on the first hash-passing tick, covered industrial does NOT ignite at the same tick, residential / commercial cells never auto-ignite, density-0 cells never auto-ignite, no double-ignition when a fire already exists at the cell, empty zones returns empty array, mixed covered + uncovered only ignites the uncovered cell, two-replay determinism. Verified `npm test` (2144/2144), `npm run build`, `npm run check:dashes`, `git diff --check`, `npx playwright test e2e/sim.spec.ts --project=chromium` (25/25). Closes the SimCity-style "neglected industrial belt catches fire" feedback loop: zoning industrial without fire-station coverage now has a real cost on top of the existing happiness-coverage penalty. Per-density auto-spawn rates, residential electrical fires, and other-disaster auto-spawns stay deferred. PR #N.
- 2026-05-06: REQ-105 slice 9 of N landed (drive-mode disaster visualization; section flips to `done`). Files: `src/app/[slug]/DriveSceneClient.tsx` (new render block walks `simState.disasters.active` and adds a kind-distinct primitive mesh at each disaster's anchor cell: fire = warm-orange cone with strong emissive (flame), flood = translucent blue plane lying flat at the cell (water level), tornado = tall dark cylinder narrowing toward the ground (funnel), earthquake = brown half-sphere dome (dust cloud), monster = purple body box + emissive head sphere group; the effect dependency array gains `simState.disasters` so place / expire events refresh the scene the next render). Tests: drive + sim e2e suites pass unchanged (26/26 local). The visible payoff is a screenshot regression target rather than a programmatic Playwright assertion. Verified `npm run type-check`, `npm test` (2123/2123 unit), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/drive.spec.ts e2e/sim.spec.ts --project=chromium` 26/26 local. Section now flips to `done`: substrate (slice 1) + palette (slice 2) + fire spread (slice 3) + fire damage (slice 4) + flood damage (slice 5) + earthquake happiness (slice 6) + tornado damage (slice 7) + monster damage (slice 8) + drive visualization (slice 9). Future polish (deferred): per-disaster animation (fire flicker, tornado spin, water-level rise, dust shake), per-disaster audio cues, automatic spawn probabilities (current substrate only fires `spawnDisaster` from the editor palette; the GDD also calls for stochastic spawning from industrial density / missing fire coverage / seasonal flood probability). PR #N.
- 2026-05-06: REQ-105 slice 8 of N landed (deterministic monster damage = combined density-drop + infrastructure-erase; drive-mode visualization still outstanding). Files: `src/lib/sim/state.ts` (new `MONSTER_DAMAGE_PROBABILITY_PER_TICK = 0.08`, between fire 0.05 and tornado 0.10; expected ~6 hits per 80-tick monster), `src/lib/sim/monsterDamage.ts` (new pure module: `monsterDamageHash` 32-bit mixing function with mixing constants distinct from the other disaster hashes; `applyMonsterDamage(buckets, disasters, tick)` walks active monsters and on a hit drops the cell's density by 1 if zoned AND erases all sim-state infrastructure at the anchor cell: power line + plant, water source + pipe + treatment plant, service building; the buckets argument groups all four affected layers (zones, power, water, services) so the caller doesn't need to thread four return values; identity-on-no-change short-circuits otherwise). `src/lib/sim/events.ts` (`applyTick` calls `applyMonsterDamage` AFTER tornado damage and BEFORE the economy reducer so a cell hosting both takes both checks; the monster's density-drop reads the post-flood/fire-damaged zones bucket so a cell already at 0 is a no-op for the monster). Tests: `tests/lib/sim/monsterDamage.test.ts` ships 12 cases covering: probability constant in (0,1), `monsterDamageHash` produces uniform [0,1) / is deterministic / produces different values for adjacent inputs, `applyMonsterDamage` empty / non-monster-disaster / zone density drop / power line + plant erase / water source + pipe + treatment plant erase / service building erase / miss preserves buckets / replay determinism over 100 ticks. Verified `npm run type-check`, `npm test` (2123/2123), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 22/22 local. Damage application now ships for all 5 disaster kinds (fire / flood / earthquake / tornado / monster). Slice 9+ lands the drive-mode visualization (per-disaster mesh overlays so the player can see the disaster effect from inside their car). PR #N.
- 2026-05-06: REQ-105 slice 7 of N landed (deterministic tornado damage erases sim-state infrastructure; monster damage + drive visualization still outstanding). Files: `src/lib/sim/state.ts` (new `TORNADO_DAMAGE_PROBABILITY_PER_TICK = 0.1`, higher than fire / flood because tornados are short-lived 40-tick disasters and damage is visually dramatic), `src/lib/sim/tornadoDamage.ts` (new pure module: `tornadoDamageHash` 32-bit mixing function with mixing constants distinct from the other disaster hashes; `applyTornadoDamage(buckets, disasters, tick)` walks active tornados and on a hit erases every piece of sim-state infrastructure at the anchor cell: power line + plant, water source + pipe + treatment plant, service building; identity-on-no-change short-circuits otherwise; the buckets argument groups the three affected layers so the caller doesn't need to thread three return values). Note: street pieces (`City.pieces`) live outside the sim engine so tornados cannot erase streets in this slice; v1 scopes to sim-state infrastructure only. `src/lib/sim/events.ts` (`applyTick` calls `applyTornadoDamage` BEFORE the economy reducer so the post-erase line / plant / service counts feed the maintenance and income calc on the same tick the erase happens; the return state's `power` / `water` / `services` reflect the post-tornado buckets). Tests: `tests/lib/sim/tornadoDamage.test.ts` ships 13 cases covering: probability constant in (0,1), probability higher than fire damage, `tornadoDamageHash` produces uniform [0,1) / is deterministic / produces different values for adjacent inputs, `applyTornadoDamage` empty / non-tornado-disaster / erase-power-line / erase-power-plant / erase-water-source-pipe-treatment-plant / erase-service-building / miss-preserves-buckets / replay determinism over 100 ticks. Verified `npm run type-check`, `npm test` (2111/2111), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 22/22 local. Slice 8 lands monster damage (a moving tornado-like effect that destroys arbitrary infra). Slice 9+ lands the drive-mode visualization. PR #N.
- 2026-05-06: REQ-105 slice 6 of N landed (earthquake happiness penalty; tornado / monster damage still outstanding). Files: `src/lib/sim/state.ts` (new `EARTHQUAKE_HAPPINESS_PENALTY = 25` constant), `src/lib/sim/events.ts` (extended `computeCityHappiness` to take a `disasters: DisastersBucket` parameter and subtract `EARTHQUAKE_HAPPINESS_PENALTY` per active earthquake from the base score; `applyHappinessTick` signature gains the same parameter; `applyTick` now runs `applyDisasterTick` BEFORE `applyHappinessTick` so an expiring earthquake's penalty drops off cleanly on the same tick the earthquake would expire). Cumulative earthquake damage stacks: two simultaneous earthquakes drop happiness by 50; five drop it to 0 (clamped). Tests: `tests/lib/sim/events.test.ts` adds 5 cases under "earthquake happiness penalty (REQ-105 slice 6)" covering: single active earthquake drops happiness to 75, two stack their penalties to 50, happiness rebounds to 100 when the earthquake expires after 20 ticks, five-stack floors at 0, two-replay determinism over a mixed-tick event log. Verified `npm run type-check`, `npm test` (2098/2098), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 22/22 local. Slice 7 lands tornado damage (erases pieces along the tornado's path) and monster damage (destroys arbitrary infra). Slice 8+ lands the drive-mode visualization. PR #N.
- 2026-05-06: REQ-105 slice 5 of N landed (deterministic flood damage erodes zone density at a slower rate than fire; tornado / earthquake / monster damage still outstanding). Files: `src/lib/sim/state.ts` (new `FLOOD_DAMAGE_PROBABILITY_PER_TICK = 0.03`, lower than the 0.05 fire rate to balance the longer 120-tick default flood duration), `src/lib/sim/floodDamage.ts` (new pure module: `floodDamageHash` 32-bit mixing function with different multipliers from fire damage / spread hashes so the rolls are independent; `applyFloodDamage(zones, disasters, tick)` walks active floods and on a hit drops the cell's density by 1 if zoned and density > 0; identity-on-no-change short-circuits otherwise). `src/lib/sim/events.ts` (`applyTick` now applies fire damage first then flood damage on the same source zones bucket so a cell hosting both takes both checks; running flood after fire matches the docstring's "fire-damage roll runs first" claim and means a cell that takes a fire-density-drop on this tick can still take a flood-density-drop in the same tick if the flood-damage roll lands). Tests: `tests/lib/sim/floodDamage.test.ts` ships 11 cases mirroring the fire-damage suite: probability constant in (0,1), probability lower than fire damage (asserted against the literal 0.05), `floodDamageHash` produces uniform [0,1) / is deterministic / produces different values for adjacent inputs, `applyFloodDamage` empty / non-flood-disaster / unzoned cell / density-0 cell / hit drops density-3 to 2 / miss preserves bucket / replay determinism over 200 ticks. Verified `npm run type-check`, `npm test` (2093/2093), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 22/22 local. Slice 6 lands tornado damage (erases pieces along the tornado's path), earthquake damage (drops a city-wide happiness penalty), and monster damage (destroys whatever it walks over). Slice 7+ lands the drive-mode visualization. PR #N.
- 2026-05-06: REQ-105 slice 4 of N landed (deterministic fire damage erodes zone density on hit cells; non-fire damage application defers to slice 5+). Files: `src/lib/sim/state.ts` (new `FIRE_DAMAGE_PROBABILITY_PER_TICK = 0.05`), `src/lib/sim/fireDamage.ts` (new pure module: `fireDamageHash(tick, row, col)` 32-bit mixing function with different multipliers than `fireSpreadHash` so the two rolls land on independent values; `applyFireDamage(zones, disasters, tick)` walks active fires, rolls the hash, and on a hit drops the cell's density by 1 if the cell is zoned and density > 0; identity-on-no-change short-circuits when no disasters / no fire-on-zoned-cell hits). Determinism: same hash-of-(tick, row, col) pattern as fire spread so two clients with the same event log derive identical density-erosion. `src/lib/sim/events.ts` (`applyTick` calls `applyFireDamage` BEFORE `maybeGrowZones` so a damage hit on this tick is not immediately replenished by a same-tick growth advance). Tests: `tests/lib/sim/fireDamage.test.ts` ships 12 cases covering: probability constant in (0,1), `fireDamageHash` produces uniform [0,1) / is deterministic / produces different values for adjacent inputs, `applyFireDamage` empty / non-fire-disaster / unzoned cell / density-0 cell / hit drops density-3 to 2 / miss preserves bucket / replay determinism over 200 ticks / density-1 to 0 burnout. Verified `npm run type-check`, `npm test` (2072/2072), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 21/21 local. Slice 5 lands per-disaster damage for the other four kinds: flood inundates zoned cells (drops density on residential, drains treasury), tornado erases pieces along its path, earthquake drops a city-wide happiness penalty, monster destroys whatever it walks over. Slice 6 lands the drive-mode visualization. PR #N.
- 2026-05-06: REQ-105 slice 3 of N landed (deterministic fire spread mechanic; per-disaster damage application defers to slice 4). Files: `src/lib/sim/state.ts` (new `FIRE_SPREAD_PROBABILITY_PER_TICK = 0.05` constant; per-tick spread roll), `src/lib/sim/fireSpread.ts` (new pure module exporting `fireSpreadHash(tick, row, col)` 32-bit mixing function that hashes `(tick, row, col)` into a uniform `[0, 1)` value with no external state, and `computeFireSpread(disasters, tick, services)` that walks every active fire, rolls the hash, and on hit picks one of the 4 cardinals via a second deterministic bin from the same roll; gates against existing-fire duplicates AND against fire-station coverage so a covered cell cannot catch). Determinism: the hash has no rng state so two clients replaying the same event log derive identical spread outcomes. `src/lib/sim/events.ts` (`applyDisasterTick` signature extended with `tick` + `services` parameters; after the `ticksRemaining` decrement it calls `computeFireSpread` on the survivors and appends spawned fires; identity-on-no-change short-circuits when the active array is empty before the decrement; `applyTick` passes `nextTick` and `state.services` through). Tests: `tests/lib/sim/fireSpread.test.ts` ships 11 cases covering: probability constant in (0,1), `fireSpreadHash` produces uniform [0,1) for many inputs / is deterministic / produces different values for adjacent inputs; `computeFireSpread` empty / non-fire-disaster / produces hits over many ticks (>0 over 200 ticks) / replay determinism / does not spread to existing-fire cells / does not spread onto fire-station-covered cells (radius 6) / spawned fires carry the per-kind default duration. Verified `npm run type-check`, `npm test` (2060/2060), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 21/21 local. Slice 4 lands per-disaster damage application: fire on a populated cell drops residents per tick (or zeros density), flood inundates zoned cells, tornado erases pieces along its path, earthquake drops a city-wide happiness penalty, monster destroys whatever it walks over. Slice 5 lands the drive-mode visualization. PR #N.
- 2026-05-06: REQ-105 slice 2 of N landed (editor Disasters palette + per-cell overlay; per-disaster damage application defers to slice 3). Files: `src/app/[slug]/edit/editorState.ts` (extended `PaletteCategory` to include `'disaster'`; new `DisasterPaletteToolType` union (5 entries: fire / flood / tornado / earthquake / monster), `DISASTER_PALETTE` constant, `DEFAULT_DISASTER_TOOL = 'fire'`), `src/app/[slug]/edit/EditorClient.tsx` (added `selectedDisasterTool` state, extended the tablist + label-resolver to include 'disaster' (Disasters label), added a per-category palette branch rendering 5 disaster buttons with kind-distinct colors (fire warm red, flood blue, tornado neutral brown, earthquake earth, monster purple), extended `handleCellClick` to dispatch a `SpawnDisasterEvent` with the selected kind on click; erase mode is a no-op for the disaster category since disasters self-expire via `applyDisasterTick`), `src/app/[slug]/edit/SnapGridView.tsx` (new optional `disasters?: DisastersBucket | null` prop; renders each `disasters.active` entry as an inset 4px overlay rect with `editor-disaster-overlay` testid + `data-disaster-kind` / `data-disaster-row` / `data-disaster-col` / `data-disaster-ticks-remaining` attributes; rendered between the water-source layer and the connector-glyph layer so the place / erase ghosts and open-end warnings stay legible). Tests: `e2e/sim.spec.ts` adds 1 new case "Disasters tab spawns a fire and renders an overlay" that switches to Disasters category, asserts all 5 tools visible, asserts fire is the default selected, paints fire at (1, 2), asserts the overlay renders with the correct kind. Verified `npm run type-check`, `npm test` (2049/2049 unit pass; no new unit cases since this slice is pure wiring), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 21/21 local. Slice 3 lands per-disaster damage application: fire spreads to 4-adjacent cells with a per-tick spread-probability gated by missing fire coverage, flood inundates zoned cells (drops density and tanks happiness), tornado erases pieces along its path, earthquake drops a coverage-wide happiness penalty, monster destroys whatever it walks over. Slice 4 lands the drive-mode visualization (red flicker for fire, blue overlay for flood, etc). PR #N.
- 2026-05-06: REQ-105 substrate slice 1 of N landed (schema + spawnDisaster event + per-tick lifetime decrement; visible payoff defers to slice 2). Files: `src/lib/sim/state.ts` (new `DisasterKindSchema` enum (fire / flood / tornado / earthquake / monster); new `DISASTER_DEFAULT_DURATION_TICKS` per-kind durations (fire 60, flood 120, tornado 40, earthquake 20, monster 80); strict `DisasterSchema` carrying `{ kind, row, col, ticksRemaining }`; tightened `DisastersBucketSchema` from passthrough to strict `{ active: Disaster[] }`; new `EMPTY_DISASTERS_BUCKET` frozen with empty `active`; `EMPTY_SIM_STATE.disasters` points at it instead of `Object.freeze({})`), `src/lib/sim/events.ts` (new strict `SpawnDisasterEventSchema` with kind / row / col payload (replaces the old `PlaceholderLayerEventSchema` enum which only had `spawnDisaster` left in it; the placeholder is fully removed since every layer now has its strict event); routed in the `SimEventSchema` discriminated union; new `applySpawnDisaster` reducer appends a fresh disaster with `ticksRemaining = DISASTER_DEFAULT_DURATION_TICKS[kind]` (no overlap rejection in v1: two disasters at the same anchor coexist); new exported `applyDisasterTick(disasters)` reducer decrements `ticksRemaining` on every active disaster and removes entries that hit 0, identity-on-no-change short-circuits when no disasters are active; wired into `applyTick` after the happiness reducer). Tests: `tests/lib/sim/state.test.ts` updates the legacy "every passthrough per-layer bucket as an empty object" assertion to a comment-only no-op (no buckets remain on passthrough; the disasters bucket gains its own strict-shape assertion + a "rejects unknown fields" negative case). `tests/lib/sim/events.test.ts` renames the legacy "layer-specific events (forward-compat)" describe to "spawnDisaster + per-tick lifetime (REQ-105 substrate slice 1)" with 6 cases: appends with default duration, allows two disasters at one anchor, per-tick decrement, removal at 0, two-replay determinism over a mixed event log, tick is identity on disasters bucket when no active disasters. Verified `npm run type-check`, `npm test` (2049/2049), `npm run build`, `npm run check:dashes`, `git diff --check` all green. Slice 2 lands the visible payoff: per-tick damage application (fire spreads to neighbor cells, flood inundates zoned cells, tornado erases pieces along its path, earthquake drops a happiness penalty per cell, monster destroys whatever it walks over) plus a drive-mode visualization (flickering red overlay for fire, blue overlay for flood, etc). Slice 3 lands UI / button / spawn-from-editor surface. PR #N.
