# 6. City Schema and Versioning

**Status:** partial

VibeCity stores each city as a single JSON payload validated by `CitySchema`. The schema is the contract between the editor (write side), persistence (`docs/gdd/03-persistence.md`), and the drive view (read side). Strict on unknown fields: any extra key fails validation, so the editor cannot smuggle in state that the drive view does not understand.

This file is the canonical spec for the city schema requirements (REQ-012, REQ-013, REQ-059, REQ-063, REQ-064).

## Top-level shape

```ts
City = {
  pieces: Piece[]            // 0..MAX_PIECES_PER_CITY (256)
  buildings: Building[]      // 0..MAX_BUILDINGS_PER_CITY (512)
  mood?: CityMood            // optional, NOT included in version hash
}
```

An empty city (`{ pieces: [], buildings: [] }`) is the v1 starting state for a fresh slug. The constant `EMPTY_CITY` is exported from `src/lib/schemas.ts` so REQ-015 (kv unset fallback) and REQ-010 (fresh-slug landing) can return it without reconstructing.

## Pieces

Street pieces describe the drivable grid. Each piece has a `type`, a `(row, col)` anchor, a 90deg `rotation`, and an optional multi-cell `footprint` (REQ-059, defaults to single-cell).

Piece type taxonomy in v1 (REQ-017, REQ-018, REQ-019, REQ-058, REQ-060, REQ-061, REQ-062):

- `straight`, `left90`, `right90` (REQ-017)
- `scurve`, `scurveLeft`, `sweepRight`, `sweepLeft` (REQ-018)
- `megaSweepRight`, `megaSweepLeft` (REQ-058, ported from VibeRacer PR #80)
- `hairpin` (REQ-060, ported from VibeRacer PR #81)
- `arc45` (REQ-061, ported from VibeRacer): bridges a cardinal connector to a corner connector. At rotation 0, connector ports are S (cardinal) and NE (corner). Four cardinal rotations supported. This is the transition piece that lets cardinal-only runs hand off to diagonal runs and back.
- `diagonal` (REQ-062, ported from VibeRacer): chains corner-to-corner across one cell. At rotation 0, connector ports are SW and NE. Four cardinal rotations supported (180deg rotations are geometrically equivalent but accepted for consistency). Length is `CELL_SIZE * sqrt(2)` once geometry lands.
- `intersection` (REQ-019, VibeCity-specific 4-way junction extending VibeRacer's planned 3-connector junction)

The schema accepts every piece type in the planned v1 taxonomy. Hiding a piece from the palette UI is a separate concern; the schema does not gate placeability beyond the type enum.

`PieceFootprintCell` carries `dr` and `dc` integer offsets relative to the piece anchor. The schema is `.strict()` so a footprint cell cannot smuggle additional fields (e.g. weights). Future Phase 0 work (REQ-063 8-direction connectors, REQ-064 segment-based path) layers on top of this footprint without changing the schema's surface.

## Buildings

Buildings are placeholder primitive types in v1 (Q-004 default B). Four enum members:

- `small-house`, `mid-house`, `shop`, `factory`

Each building has a `(row, col)` anchor and a 90deg `rotation`. v1 buildings are single-cell with no simulation behavior (REQ-028, REQ-029, REQ-030); they are visual variety for the drive view. Multi-cell building footprints (Q-004 option C) are out of scope for v1.

## Mood

`CityMood` carries optional `timeOfDay` and `weather` fields. Both are short strings in v1; the schema does not enum-check them because the lighting / weather modules have not yet been ported. When the lighting port lands, this section will tighten the types to mirror VibeRacer's `TimeOfDaySchema` and `WeatherSchema`.

Mood is excluded from the version hash (REQ-013). Adding or changing the mood on an existing city does NOT produce a new content version, so prior version references remain stable.

## Versioning (REQ-013)

`hashCity(city)` (REQ-013, future slice) computes a stable hash over `pieces` and `buildings`. Mood is excluded. Hash format mirrors VibeRacer's `hashTrack` (a 64-char hex digest); see `kvKeys.cityVersion` in `src/lib/kv.ts` and `CityVersionHash` for the brand type.

Hash stability requirements:

- Equal `pieces` and `buildings` produce equal hashes regardless of object key order or array reference identity.
- Adding or removing a `footprint` key on a piece that does not change the resolved footprint must NOT change the hash. The future `hashCity` implementation canonicalizes by computing the resolved footprint, not the raw `footprint` field.
- Mood changes do not change the hash.

REQ-013 ships with its own slice and tests; this section will gain a Build log entry when it lands.

## Limits

| Limit | Value | Rationale |
| --- | --- | --- |
| `MAX_PIECES_PER_CITY` | 256 | Enough for a v1 city loop and a few side streets. Bump in a follow-up if playtest reveals city-scale pressure. |
| `MAX_BUILDINGS_PER_CITY` | 512 | Two buildings per piece on average is plenty for visual fill. |

VibeRacer's `MAX_PIECES_PER_TRACK` is 64 because a track is a closed loop. VibeCity's pieces form an open street network and need more room.

## Out of scope for v1

- Per-piece metadata (color, decorations, custom material). Pieces are placeholder visuals in v1.
- Building footprints beyond 1x1 (Q-004 default B fences this).
- Per-city lighting overrides beyond `timeOfDay` / `weather`.
- Schema migration tooling. v1 is small enough that breaking changes can be handled by namespace bumps in `city:` keys.
- Versioning of mood-only changes (mood is excluded from the hash by design).

### Build log

- 2026-05-04: REQ-064 (segment-based path substrate) gained a one-shot summary projection. Files: `src/lib/trackPath.ts` (new `TrackPathSummary` interface carrying `mainSegmentLength` / `totalPieces` / `mainSegmentClosesLoop` / `segmentCount`; new `summarizeTrackPath(path)` helper that reads the segment-based substrate and projects it into the flat shape so callers (the editor toolbar readout, future drive-mode HUD, future save-time integrity check) can render or branch on the connected-graph topology without iterating segments themselves; `totalPieces` counts unique pieces across every segment deduped by reference identity because the deterministic intersection pass-through can revisit the same piece from a sibling arm seeded as a new segment, so summing segment lengths would over-count; empty path returns zeros and `mainSegmentClosesLoop: false`; the main segment is always `path.segments[0]` per `buildTrackPath`'s contract), `tests/lib/trackPath.test.ts` (10 new cases under `summarizeTrackPath` covering empty city, single straight one-piece open main segment, two-piece open main segment, 2x2 closed loop with `mainSegmentClosesLoop: true`, disconnected components giving `mainSegmentLength < totalPieces`, three isolated pieces with `segmentCount = 3`, multi-cell hairpin counted as one piece, total piece dedup across segments, no-mutation guard, the intersection-wired-four-straights walker bound, and a CitySchema round-trip). The new helper is the first consumer of `path.segments[0]` and `path.segments[i].closesLoop` and lands ahead of the drive-mode integrity HUD which will reuse the same shape. PR #N.
- 2026-05-04: REQ-064 (segment-based path substrate) gained the cross-piece connector validator. Files: `src/lib/trackPath.ts` (new `UnmatchedPort` interface carrying `pieceIndex` / `cellRow` / `cellCol` / `dir`, new `validateConnections(city)` helper that walks every placed piece's connector ports through `connectorPortsOf` and returns the list of ports facing empty space, the grid edge, or a neighbor cell that does not expose an opposing port; the helper builds a port index keyed by `cellKey:dir` once and threads it through the per-port classifier so the cost stays O(N) over the total port count instead of O(N^2) over all piece pairs; the `neighborOwner !== pieceIndex` guard prevents an intersection's own opposing arm from matching itself across the same source cell; results are walked in placement order with ports in `connectorPortsOf` order so a UI consumer can rely on a stable diff order; the helper returns substrate data not pixel-space glyphs so it lives in `src/lib/` rather than `src/app/[slug]/edit/connectorGlyphs.ts` and the future drive-mode warning HUD plus the future save-time integrity check both read it without dragging editor SVG state forward), `tests/lib/trackPath.test.ts` (14 new cases covering empty city, single straight reporting both ports, two stacked straights with the shared edge matched, a 2x2 closed loop of 90deg corners with zero unmatched, an alone intersection reporting four open arms, an intersection wired to four straights with the four outer ends as the only unmatched, the source-piece guard for an alone intersection, two adjacent but non-facing pieces reporting four open ports, two diagonals laid corner-to-corner with one matched and one open per piece, placement-order-not-anchor-order ordering, fresh-array-on-every-call invariant, multi-cell hairpin port coverage, an arc45 cardinal-corner pair matched against a 180deg-rotated arc45 partner, and a CitySchema round-trip). The geometry layer plus the multi-segment-per-intersection walker stay deferred to their own slices. PR #N.
- 2026-05-04: REQ-064 (segment-based path substrate) extended to a multi-segment walker. Files: `src/lib/trackPath.ts` (`buildTrackPath` now emits one `PathSegment` per connected component instead of one segment from `pieces[0]` only; new file-local helpers `segmentIdForIndex(index)` mapping the segment index to `'main'` for index 0 and `'segment-1'` / `'segment-2'` / etc. for subsequent components, `walkComponent(start, pieces, visitedAnchors)` extracting the per-component walker so the outer loop owns segment seeding and the walker owns intra-segment state and pushes every visited anchor into a shared `visitedAnchors` set so the outer loop skips already-absorbed pieces, and `findUnvisitedConnectedNeighbor(piece, port, pieces, visitedAnchors)` as the start-exit heuristic for sub-segments that prefers a port connecting to any other unvisited piece in the city; `closesLoop` is now computed by checking whether `last.exitPort` connects back to `first.piece` instead of relying on the seen-cell guard alone, so the substrate emits the explicit closed-loop signal that the future drive-side spawn / lap-counting layer (REQ-033 / REQ-037) will read; `cellToOrderIdx` stays scoped to the `'main'` segment only so existing single-segment consumers keep their backward-compatible `Map<string, number>` contract; `cellToLocators` carries entries for every segment's footprint cells so non-main pieces are addressable via the locator map), `tests/lib/trackPath.test.ts` (renamed two existing tests `'drops disconnected pieces from the segment'` to `'emits a separate segment per disconnected piece'` and `'drops a disconnected island piece from the segment'` to `'keeps a disconnected island piece out of the main segment'`; added 9 new cases including multi-component placement order, three-component segment-2 emission, per-segment locator routing, scoped cellToOrderIdx invariant, multi-cell footprint locator coverage across two isolated segments, anchor-order independence, island-first ordering, full-piece coverage invariant, and no-spurious-cross-segment-locators invariant). The geometry layer plus the multi-segment-per-intersection walker (where one intersection emits a segment per each of its arms) plus the cross-piece connector validator stay deferred to their own slices. PR #N.
- 2026-05-04: REQ-064 (segment-based path substrate) flipped not_started to partial. Files: `src/lib/trackPath.ts` (new pure module exporting the connected-graph helpers `portCell`, `neighborAnchorCell`, `portsConnect`, `findConnectedNeighbor` ported from VibeRacer's `track.ts` Phase 0d substrate plus the `OrderedPiece`, `PathLocator`, `PathSegment`, `TrackPath` types and the `buildTrackPath(city)` builder; the builder walks the city's pieces from `city.pieces[0]` preferring the start exit port that connects to the second piece when pieces.length >= 2 and falling back to the second port from `connectorPortsOf` otherwise, follows matched ports forward via `findConnectedNeighbor`, terminates on dead end or loop closure, and for the 4-way `intersection` picks the port directly opposite the entry as the next exit so a pass-through walk reads as the natural continuation; non-pass-through branching at intersections is deferred to the multi-segment walker slice; an empty city returns an empty `TrackPath` with zero segments rather than throwing because VibeCity ships an empty city by default per REQ-010 / REQ-015; multi-cell pieces (mega sweep, hairpin) emit one locator per footprint cell into `cellToLocators` so the future drive-side wheel-contact slice (REQ-065) can read every footprint cell of a placed piece; geometry (sampled centerlines, headings, world-space center, spawn / finish line, checkpoints) is intentionally absent and waits for F-003 / F-004 / REQ-031 / REQ-032), `tests/lib/trackPath.test.ts` (31 unit cases covering the helpers and the builder including empty city, single piece, two-piece chain, multi-cell footprint locator emission for hairpin and mega sweep, intersection pass-through, disconnected island drop, anchor-cell-only `cellToOrderIdx` invariant, fresh-path-on-every-call invariant, and `CitySchema` round-trip). The substrate ships ahead of REQ-019 (intersection runtime branching), REQ-032 (drive-side wheel contact), and REQ-065 (multi-locator candidate evaluation) which all consume this vocabulary; the geometry layer plus the multi-segment walker land in their own slices. PR #N.
- 2026-05-04: REQ-063 substrate gained its first runtime consumer: the editor SVG renders connector glyphs at every placed piece's ports. Files: `src/app/[slug]/edit/connectorGlyphs.ts` (new pure module that maps `connectorPortsOf(piece)` through a half-cell step along the compass direction to compute pixel-space glyph centers; exports `CELL_HALF_PIXELS`, `GLYPH_RADIUS_PIXELS`, the `ConnectorGlyphKind` union, the `ConnectorGlyph` shape, `pieceConnectorGlyphs` / `cityConnectorGlyphs` resolvers, and the `CONNECTOR_DIR_LABEL` compass-label map), `src/app/[slug]/edit/SnapGridView.tsx` (renders one `<circle>` per glyph with cardinal vs corner fill colors and `pointerEvents='none'` so the markers stay non-interactive; new `data-connector-count` attribute mirrors the glyph total), `tests/app/connectorGlyphs.test.ts` (23 cases), `e2e/editor.spec.ts` (`/connector-spec/edit` spec). The cross-piece connector validator (the "this piece's east port faces that piece's west port" check that produces a drivable city) still waits for REQ-064 (segment-based path) per the original deferral. PR #N.
- 2026-05-04: REQ-063 (8-direction connector substrate) landed. Files: `src/lib/connectors.ts` (new module exporting `Dir` (`0..7` clockwise from north), `DIR_N` / `DIR_NE` / ... / `DIR_NW` named constants, `DIR_OFFSETS` table, `opposite(d) = (d + 4) % 8`, `isCardinal(d)` / `isCorner(d)` classifiers, `ConnectorPort` interface carrying footprint-cell offset and direction, `connectorPortsOf(piece)` resolving the rotated ports for every piece type in the v1 taxonomy including the VibeCity-specific 4-way `intersection` (REQ-019) and the multi-cell `hairpin` whose ports live at footprint offsets `(-1, 0)` and `(1, 0)` per VibeRacer's 2x3 canonical layout, `connectorsOf(piece)` returning just the directions, and `portsFaceEachOther(a, b)` enforcing the cardinal-vs-cardinal and corner-vs-corner pairing rule), `tests/lib/connectors.test.ts` (52 cases covering the constants, the offset table's opposite-cancellation invariant, opposite as an involution, the classifier partition, the per-piece-type port mapping at every cardinal rotation including the cardinal-only block, the cardinal-and-corner mix on `arc45`, the all-corner pair on `diagonal`, the 4-way symmetry on `intersection`, the multi-cell hairpin port offsets and their `-0` collapse on rotation, the fresh-array contract, and the `portsFaceEachOther` truth table including cardinal-vs-corner cross rejection). The substrate ships ahead of REQ-064 (segment-based path) and REQ-065 (multi-locator wheel contact) which both consume this vocabulary; the validator across two adjacent placed pieces (the "this piece's east port faces that piece's west port" check) lives in its own slice when REQ-064 lands so the in-editor and persisted-city validity surface stays a single landing instead of a half-state. The hairpin port offsets `(-1, 0)` / `(1, 0)` assume the canonical 2x3 footprint that arrives with REQ-060; the connector substrate names them now so REQ-060 lands without reaching back to retrofit the direction module. PR #N.
- 2026-05-04: REQ-058 (mega sweep multi-cell footprint) and REQ-059 (multi-cell footprint plumbing, partial) landed. Files: `src/app/[slug]/edit/snapGrid.ts` (new `MEGA_SWEEP_RIGHT_FOOTPRINT` and `MEGA_SWEEP_LEFT_FOOTPRINT` constants encoding the canonical 2x2 block anchored at the bottom-right and bottom-left respectively; new `defaultFootprintForPiece(piece)` resolver that returns the rotated canonical footprint for `megaSweepRight` / `megaSweepLeft` and the single-cell default for every other type; `pieceFootprintCells` now falls back to the resolver when `piece.footprint` is undefined so every existing helper inherits the multi-cell behavior automatically), `src/app/[slug]/driveScene.ts` (mirrored constants and resolver locally per the established duplication pattern that keeps the drive scene module free of the editor dependency tree; `pieceFootprintWorldCells` reads the type-driven default so a placed mega sweep renders all four ground quads). The persistence shape stays minimal: `placePiece` does not record an explicit `footprint` field for a placed mega sweep so the canonical hash treats two mega sweeps with the same `(type, row, col, rotation)` as identical. The hash function `hashCity` (REQ-013) needs no edits because it already canonicalizes by treating an omitted footprint as the single-cell default; a placed mega sweep with an omitted footprint hashes by `{ type, row, col, rotation }` alone and a hand-edited mega sweep that records its own canonical 4-cell footprint will hash differently (a future slice can extend `hashCity` to canonicalize an explicit footprint that matches the type-driven default to omitted; v1 does not need this because the editor never records an explicit footprint). PR #N.
- 2026-05-03: REQ-061 (`arc45`) and REQ-062 (`diagonal`) piece types added to `PieceTypeSchema`. Files: `src/lib/schemas.ts` (extended `PieceTypeSchema` enum from 11 to 13 members; comment block expanded to describe the cardinal-to-corner bridge role of arc45 and the corner-to-corner role of diagonal), `tests/lib/schemas.test.ts` (added cases for both new types at every cardinal rotation, optional single-cell footprint on arc45, rejection of non-cardinal rotations, and a CitySchema scenario chaining `straight -> arc45 -> diagonal -> diagonal -> arc45`). Mirrors the VibeRacer 2026-05-03 port. Connector validation, sampled centerlines, wheel contact, pace notes, and difficulty scoring stay deferred to FOLLOWUPS (no driving in VibeCity yet); the schema lands first so saved cities can record arc45 / diagonal placements ahead of the runtime port. PR #N.
- 2026-05-03: REQ-013 landed. Files: `src/lib/hashCity.ts` (`hashCity`, `canonicalCityJson`, internal `normalizedFootprint` / `isDefaultFootprint`), `tests/lib/hashCity.test.ts` (18 cases covering format, determinism, mood exclusion, footprint canonicalization, change detection). Mood is excluded from the digest. Footprint canonicalization dedupes, collapses `-0` to `0`, sorts by `(dr, dc)`, and omits the field when it resolves to the single-cell default. PR #N.
- 2026-05-03: REQ-012 landed. Files: `src/lib/schemas.ts` (`PieceTypeSchema`, `RotationSchema`, `PieceFootprintCellSchema`, `PieceSchema`, `BuildingTypeSchema`, `BuildingSchema`, `CityMoodSchema`, `CitySchema`, `EMPTY_CITY`, `MAX_PIECES_PER_CITY`, `MAX_BUILDINGS_PER_CITY`), `tests/lib/schemas.test.ts` (city / piece / building / mood cases). Verified `npm run type-check`, `npm run test`, `npm run build` all green. Dash check clean. PR #N.
