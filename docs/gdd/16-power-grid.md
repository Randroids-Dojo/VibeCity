# 16. Power Grid

**Status:** partial

Power is the first gating utility for zone growth. A zoned cell without power growth-stalls; an existing populated cell without power browns out, citizens get unhappy, and the cell decays. Power is generated at plants, transmitted via lines, and consumed per-zone-cell.

This file is the canonical spec for the power layer (REQ-085 through REQ-089).

## What this section covers

- **REQ-085** Power plant placement. Two v1 plant types: `coal` (cheap, dirty, 100MW) and `solar` (expensive, clean, 30MW). Plants are 2x2 multi-cell footprint pieces. Placement is via a new `Power` toolbar tab in the editor, alongside Streets / Buildings.
- **REQ-086** Power lines. Power flows through dedicated power-line cells (single-cell, snap-grid, like streets). Lines have a `voltage` notion only insofar as line cells form a connected component; a connected zone cell receives power from any plant in that component. No transformer / substation chain in v1.
- **REQ-087** Connectivity solver. Each tick, the engine computes which zone cells are connected to which plants via lines, sums plant capacity per connected component, divides among demanding cells. A cell is "powered" if its component has remaining capacity; "browned out" otherwise.
- **REQ-088** Drive-mode visible signal. From the car, a powered residential cell at night shows lit windows (the existing lit-window dot, REQ-110 will land it on this signal). A browned-out cell shows dark windows. Power lines are visibly wired in the drive view.
- **REQ-089** Pollution from plants. `coal` plants emit a per-tick pollution value into adjacent cells. Pollution feeds into citizen happiness (REQ-079) and zone growth gates (REQ-081).

## Schema sketch

```ts
City.sim.power = {
  plants: Array<{ id: string; kind: 'coal' | 'solar'; row: number; col: number; capacityMW: number }>
  lines: Array<{ row: number; col: number }>      // single-cell line pieces
  cellPowerStatus: Record<string, 'powered' | 'brownout' | 'unpowered'>
  pollution: Record<string, number>               // per-cell pollution score
}
```

## Out of scope for this section

- Wind / nuclear / hydro plant types (defer to v1.1 once coal / solar feel right).
- Per-line voltage class, transformers, substations. v1 is "connected component sums capacity".
- Power outages from disasters (lives in disasters layer, REQ-105 series).

### Build log

- 2026-05-14: REQ-089 coal pollution landed under the REQ-085 power-grid umbrella. Files: `src/lib/sim/state.ts` (new `power.pollution` map plus `COAL_POLLUTION_PER_TICK`, `COAL_POLLUTION_RADIUS_CELLS`, and `POLLUTION_HAPPINESS_WEIGHT` constants), `src/lib/sim/powerPollution.ts` (new pure helper computing current-tick pollution from coal plants into the four orthogonally-adjacent cells, with overlap stacking and solar emitting none), `src/lib/sim/events.ts` (tick reducer refreshes pollution after disaster infrastructure damage, `computeCityHappiness` subtracts average populated-cell coal exposure), `src/lib/sim/tornadoDamage.ts` and `src/lib/sim/monsterDamage.ts` (preserve the map shape until the next tick refreshes it), `src/app/[slug]/edit/SnapGridView.tsx` (empty power fallback carries the strict map). Tests: `tests/lib/sim/powerPollution.test.ts`, `tests/lib/sim/state.test.ts`, `tests/lib/sim/events.test.ts`, `tests/lib/sim/tornadoDamage.test.ts`, `tests/lib/sim/monsterDamage.test.ts`. Verified `npm run check:dashes`, `git diff --check`, `npm run type-check`, targeted sim tests, `npm test`, and `npm run build` locally. REQ-085 remains partial because 2x2 plant-footprint validation is still not implemented. PR #N.
- 2026-05-06: REQ-088 follow-on landed (editor day/night mood toggle). Files: `src/app/[slug]/edit/EditorClient.tsx` (added a Day / Night button pair to the editor's sim-speed toolbar after the tick readout; clicks call `setCityWithHistory((prev) => ({ ...prev, mood: { ...prev.mood, timeOfDay: mode } }))` plus `setAutosaveStatus('pending')` when the value changes; identity-on-no-change guard returns the previous city when the mode is already active so a duplicate click does not pollute the history stack; per-mood styling (Day = light blue background, Night = deep navy) so the active mood reads at a glance), `src/app/[slug]/edit/autosaveStatus.ts` (extended `isCityContentEqual` to compare `mood` via `JSON.stringify(a.mood ?? null) !== JSON.stringify(b.mood ?? null)`; the prior implementation excluded mood to mirror REQ-013 hash semantics, but mirroring the hash here meant a Day -> Night toggle silently dropped through the autosave debounce because content-equal-by-pieces-and-buildings short-circuited the PUT before the mood ever reached the server; the new check still leaves mood OUT of `hashCity` (REQ-013 invariants stay intact) so a mood-only change does not bump the version hash; the change is scoped to the autosave trigger). Tests: `tests/app/autosaveStatus.test.ts` flipped the previous "mood-only difference is equal" case to "mood-only difference is NOT equal" (mirroring the new invariant) and added 3 cases (same-mood-on-both-sides, no-mood-on-both-sides, one-side-has-mood-other-does-not). `e2e/sim.spec.ts` adds 1 new case "day/night mood toggle flips active state and triggers autosave" that goes to /[slug]/edit, asserts Day is the default active button, clicks Night, asserts the toggle flips, asserts the autosave-status indicator settles to 'saved' (proving the PUT round-trip fired), then toggles back to Day and re-verifies the autosave settles. The PUT-body-introspection variant of the test was tried first but proved flaky on timing; the autosave-status indicator is the deterministic surface. Verified `npm run type-check`, `npm test` (1894/1894), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts e2e/editor.spec.ts --project=chromium` 38/38 local. Players can now flip a slug between day and night without API access; the Drive view honors the persisted mood on next load. PR #N.
- 2026-05-06: REQ-088 slice 2 of 2 landed (lit-windows-at-night for powered zones). Files: `src/app/[slug]/timeOfDay.ts` (new pure module: `TimeOfDay = 'day' | 'night'`, `resolveTimeOfDay(mood)` defaults to day on undefined / null / unknown, `TIME_OF_DAY_PALETTE` per-mode sky / ground / sun / ambient values (night sky `0x0e1a2c`, night ground `0x2a2f1e`, night sun 0.15, night ambient 0.25 vs day 1.0 / 0.4), `zoneEmissiveHex(timeOfDay, kind, status)` returning emissive color (day: brownout `0x222200` else 0; night: powered residential warm `0xffa040`, commercial cool `0x80c0ff`, industrial dim red `0xa04020`, brownout `0x332208`, unpowered 0), `zoneEmissiveIntensity(timeOfDay, status)` returning the multiplier (day brownout 1.0 else 0; night powered 1.6, brownout 0.8, unpowered 0)). `src/app/[slug]/DriveSceneClient.tsx` resolves time-of-day from `city.mood` at scene-mount, swaps `scene.background` and ground material to the night palette when needed, dims `AmbientLight` and `DirectionalLight` intensities to night values, and passes per-cell emissive into each zone material. The scene root mirrors `data-time-of-day`. Scene effect deps gain `city.mood` so a mood flip rebuilds. Tests: `tests/app/timeOfDay.test.ts` ships 27 cases covering `resolveTimeOfDay` defaults / canonical / unknown, `TIME_OF_DAY_PALETTE` invariants (day brighter than night across sky / ground / sun / ambient, night sun > 0 for moonlight, all intensities non-negative), `zoneEmissiveHex` per-mode-kind-status palette (day powered unlit, day brownout warning tint, night powered residential warm with red dominant, night powered commercial cool with blue >= red, night powered industrial non-zero, night brownout dim < powered), `zoneEmissiveIntensity` per-mode-status (day powered 0, day brownout 1.0, night powered > 1.0, night brownout < powered, every combination finite + non-negative). Verified `npm run type-check`, `npm test` (1891/1891), `npm run build`, `npm run check:dashes`, `git diff --check` all green; existing `e2e/drive.spec.ts` 4/4 still pass. Editor day / night UI toggle defers to a follow-on slice; the same `solvePowerStatus` helper that the editor uses for the green-stroke signal feeds the emissive palette here so the two views stay consistent. PR #N.
- 2026-05-06: REQ-088 slice 1 of 2 landed (zones visible in drive scene; lit-windows-at-night follow-on slice still deferred). Files: `src/app/[slug]/page.tsx` (drive route now reads the builder cookie via `cookies()` and passes it to `DriveSceneClient` as a new prop; mirrors the editor's pattern from the unification slice; falls back to `notFound()` if cookie missing), `src/app/[slug]/DriveSceneClient.tsx` (accepts `builderId` prop, mounts `useSimEngine` so the drive view runs its own tick stream and folds in events from the server tail; the engine cold-loads zones + power state from `/api/city/[slug]/events` on mount; opening the drive view in tab 2 alongside the editor in tab 1 still converges via the substrate's event ordering, both tabs see the same zones because they replay the same event log). The main scene effect now renders zone overlays before the street pieces: per-cell flat `PlaneGeometry` quad sized to `CELL_SIZE`, positioned at `PIECE_GROUND_LIFT / 2` so a piece quad on the same cell renders on top, per-kind color (residential green, commercial blue, industrial brown matching the editor's SnapGridView palette), per-density opacity (0.35 / 0.55 / 0.78 / 1.0 from ungrown to max), brownout zones get a subtle dark-yellow emissive tint as a v1 visual signal until slice 2 lands the lit-windows-at-night payoff. The scene effect's dep array gained `simState.zones` and `simState.power` so a fresh zone or a power-status flip triggers a scene rebuild; this is expensive (every 5s under normal growth ticks) but acceptable for slice 1; the perf optimization (incremental zone diff via separate effect with a sceneRef) defers to a follow-on if profiling shows it as hot. Tests: existing `e2e/drive.spec.ts` 4/4 still pass; the playwright webServer runs without KV so the events GET fallback returns empty zones / power and the drive scene renders identically to the pre-slice baseline. Verified `npm run type-check`, `npm test` (1864/1864), `npm run build`, `npm run check:dashes`, `git diff --check` all green. Slice 2 of REQ-088 (lit-windows-at-night via `mood.timeOfDay === 'night'` check + per-zone emissive tint based on power status) and the per-zone power status visualizations beyond the brownout-tint pile up as their own slices once they have explicit player-visible signal beyond the editor's stroke. PR #N.
- 2026-05-06: REQ-087 connectivity solver landed (slice 3 of N for the power layer; first slice where the wired grid actually powers something). Files: `src/lib/sim/powerSolver.ts` (new pure module exporting `POWER_DEMAND_PER_CELL_MW = 1` so capacity reads as "this plant powers N cells", `CellPowerStatus` union (powered / brownout / unpowered), `PowerComponent` interface carrying transmission Set + plants array + capacityMW, `computePowerComponents(power)` running BFS over the union of plant anchors and line cells with 4-direction orthogonal adjacency to discover connected components and sort them deterministically by smallest cell key, `solvePowerStatus(zones, power)` iterating zoned cells in alphabetical key order to assign powered/brownout/unpowered with capacity tracked per component for stable replay, `cellPowerStatusFor(row, col, zones, power)` convenience for single-cell lookup). The 4-direction adjacency convention is deliberate: power flows orthogonally in v1 (no diagonal connectivity, no transmission line bending). Plants are treated as single-cell anchors per the slice 1 contract; the 2x2 footprint resolution and per-arm adjacency lands when the UI slice validates plant placement against neighbors. `src/app/[slug]/edit/SnapGridView.tsx` calls `solvePowerStatus` once per render (the editor's grid is small so the recomputation is cheap; profiling can drive memoization if needed) and applies the result to each zone overlay's stroke: powered = green, brownout = yellow-olive, unpowered = gray (stays at the kind-default stroke for the unpowered case so the zone still reads as its kind-distinct color); each overlay carries `data-zone-power-status` so the e2e tests can assert visible power state. Tests: `tests/lib/sim/powerSolver.test.ts` (22 cases under `computePowerComponents` covering empty bucket, lone plant, lone line with zero capacity, plant-plus-adjacent-line, disjoint plant-and-line as separate components, multi-cell line chain, two-plants-via-line-bridge, no-diagonal-connectivity, two-replay determinism; under `solvePowerStatus` covering empty zones, all-unpowered, plant-adjacent powered, line-chain powered, line-without-plant brownout, far-away unpowered, no-diagonal powered, capacity-cap with brownout-overflow, two-replay determinism; under `cellPowerStatusFor` covering not-zoned, mirrors-solver, and unreachable-zoned). `e2e/sim.spec.ts` adds 1 new case "place plant + line + adjacent zone -> zone shows powered status" that paints a residential zone at (0, 2), asserts the overlay reads `data-zone-power-status='unpowered'`, then places a coal plant at (0, 0) and a line at (0, 1), and asserts the same overlay flips to `data-zone-power-status='powered'`. Verified `npm run type-check`, `npm test` (1864/1864), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 9/9 local. Lit-windows-at-night drive signal (REQ-088) lands in slice 4 and reads from this same solver. Pollution (REQ-089) lands in slice 5. Per-tick power computation cached on state lands as a perf slice when profiling reveals the lazy recomputation is hot. PR #N.
- 2026-05-06: REQ-085 power slice 2 of N landed (editor Power tab + plant/line tools). Files: `src/app/[slug]/edit/editorState.ts` (extended `PaletteCategory` from `'street' | 'building' | 'zone'` to also include `'power'`; added `PowerPaletteToolType` union (plant-coal / plant-solar / line), `POWER_PALETTE` constant with 3 entries, `DEFAULT_POWER_TOOL = 'line'` because most power-grid edits are running line cells), `src/app/[slug]/edit/EditorClient.tsx` (added `selectedPowerTool` state, extended the tablist to include the power tab, added a per-tab palette branch rendering the three power buttons with kind-distinct palette colors when power category is active, extended `handleCellClick` to dispatch `EraseLineEvent` (erase mode), `RunPowerLineEvent` (line tool selected), or `PlacePowerPlantEvent` with kind from selectedPowerTool, passed `simState.power` to the SnapGrid as a new optional prop), `src/app/[slug]/edit/SnapGridView.tsx` (added optional `power?: PowerBucket | null` prop; new `POWER_PLANT_FILL` / `POWER_PLANT_STROKE` constants per-kind plus shared `POWER_LINE_FILL` / `POWER_LINE_STROKE`; renders each line cell as a 50%-cell-size yellow square overlay positioned at cell center via `editor-power-line` testid plus `data-power-line-row` / `data-power-line-col` attributes; renders each plant as an `editor-power-plant` overlay 4px-inset from cell with kind-distinct fill/stroke + `data-power-plant-kind` attribute; both layers render after the zone overlays and before the connector glyphs so place / erase ghosts and open-end warnings stay legible). Tests: `e2e/sim.spec.ts` adds 2 new cases: "Power tab exposes plant + line tools and paints a line" (switch to power tab, assert all 3 tools visible, line is default selected, click origin cell, line overlay appears at that cell) and "switch to coal plant and paint, then erase a power line" (switch to coal plant, click a cell, plant overlay appears with kind=coal, switch to line, paint another cell, line overlay appears, switch to erase mode via E shortcut, click the line cell, line overlay disappears). Verified `npm run type-check`, `npm test` (1842/1842), `npm run build`, `npm run check:dashes`, `git diff --check` all green, `npx playwright test e2e/sim.spec.ts --project=chromium` 8/8 local. The connectivity solver (REQ-087, slice 3) makes the placed plants and lines actually power something; slice 2 is the wiring tool. Per-cell power status (REQ-088) and lit-windows-at-night drive signal land in slice 4. PR #N.
- 2026-05-06: REQ-085 power slice 1 of N landed (schema + place/erase events + reducer; second sim layer to plug into the substrate after REQ-080 zoning). Files: `src/lib/sim/state.ts` (new `PowerPlantKindSchema` enum (coal / solar), `POWER_PLANT_CAPACITY_MW` constant Record (coal 100 MW, solar 30 MW per spec), strict `PowerPlantSchema` with kind/row/col, strict `PowerBucketSchema` with `plants: PowerPlant[]` and `lines: Record<"row,col", true>`, `EMPTY_POWER_BUCKET = { plants: [], lines: {} }` frozen at module load, `powerLineKey(row, col)` returning `"row,col"` matching the existing zoneCellKey / streetCellSet convention; `EMPTY_SIM_STATE.power` now points at `EMPTY_POWER_BUCKET` instead of `{}`), `src/lib/sim/events.ts` (new strict `PlacePowerPlantEventSchema` carrying `(kind, row, col)`, `RunPowerLineEventSchema` carrying `(row, col)`, `EraseLineEventSchema` carrying `(row, col)`; all three move into the `SimEventSchema` discriminated union; the `PlaceholderLayerEventSchema` enum drops `placePowerPlant` / `runPowerLine` / `eraseLine` since the typed variants now route them; the placeholder list shrinks to `placeWaterSource` / `placeServiceBuilding` / `spawnDisaster`. New reducer cases: `applyPlacePowerPlant` appends plants to the array (idempotent on duplicate-anchor-and-kind clicks; allows multiple plants of different kinds at the same anchor for v1, stricter overwrite semantics defer to a follow-on if playtest reveals the need); `applyRunPowerLine` adds a line cell (identity on duplicate); `applyEraseLine` removes a line cell (identity when no line at cell). Multi-cell plant footprints (REQ-085 spec text 2x2) record only the anchor cell in slice 1; the resolution lands with the UI slice (slice 3) which needs footprint validation against pieces / buildings / lines / other plants. Connectivity solver (REQ-087) lands in slice 2; per-cell powered/brownout/unpowered classification + lit-windows-at-night drive signal (REQ-088) lands in slice 4 once the solver is in place. Tests: `tests/lib/sim/state.test.ts` adds 18 new cases under PowerPlantKindSchema (accepts coal/solar, rejects nuclear/wind), POWER_PLANT_CAPACITY_MW (coal 100, solar 30, coal > solar invariant), PowerPlantSchema (origin, negative coords, strict-extras-rejected, non-integer-coords-rejected), PowerBucketSchema (empty/populated/strict-extras/bad-line-value), EMPTY_POWER_BUCKET (shape/schema/frozen), powerLineKey (matches zoneCellKey convention). `tests/lib/sim/events.test.ts` adds 13 new cases under placePowerPlant (appends to empty bucket, identity on duplicate, allows-different-kinds-at-same-anchor for v1, multi-plant ordering, no-mutation, preserves zones/other buckets) and runPowerLine + eraseLine (single line add, duplicate identity, multi-line, eraseLine identity-on-missing, eraseLine removes-targeted, only-removes-targeted, two-replay determinism over a mixed power-event log); the existing forward-compat layer test was reframed to placeServiceBuilding (REQ-100 not landed yet). Verified `npm run type-check`, `npm test` (1842/1842), `npm run build`, `npm run check:dashes`, `git diff --check` all green. REQ-085 flips not_started to partial. UI tools (Power tab in editor with plant/line buttons), 2x2 footprint validation, connectivity solver (REQ-087), per-cell power-status calc, lit-windows drive signal (REQ-088), pollution (REQ-089) all stay deferred to their own dots. PR #N.
