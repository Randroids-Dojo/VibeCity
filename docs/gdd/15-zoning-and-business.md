# 15. Zoning and Business

**Status:** partial

Zoning is how the player tells the sim "this area is for housing" / "this area is for shops" / "this area is for factories". The sim then grows buildings into the zoned cells based on demand from the citizens and economy layers. Business is the commercial and industrial layer that consumes labor and produces tax revenue.

This file is the canonical spec for the zoning + business layer (REQ-080 through REQ-084).

## What this section covers

- **REQ-080** Zoning categories. Three zone types replace the existing four placeholder building types: `residential`, `commercial`, `industrial`. Each is paintable on the sim grid in any cell that does NOT contain a street piece. Density is implicit in v1 (every zone cell starts low-density and grows; no separate low/medium/high paint).
- **REQ-081** Zone-to-building growth. Each tick, a zoned cell with met demand from the population layer grows a building. The building primitive replaces the existing flat building cell render: `residential` grows `small-house` then `mid-house` then `apartment` (new); `commercial` grows `shop` then `mall` (new); `industrial` grows `factory` then `plant` (new). Growth is one density step per N ticks.
- **REQ-082** Demand readout. The sim-as-primary view (REQ-110 series) surfaces the current R / C / I demand bars (SimCity 2000 standard). Demand is a function of population, available power, available water, services, and economy.
- **REQ-083** Job slots. Commercial and industrial cells expose job slots proportional to their density. Population layer trip demand reads the nearest open job slot.
- **REQ-084** Schema and persistence. Zones live on the existing `city.buildings` array as a discriminated union of zoned vs flat-placeholder cells; the v1 placeholder building types stay supported during a migration period (a city saved before the sim layer landed should still load and render).

## Schema sketch

```ts
Zone = {
  kind: 'residential' | 'commercial' | 'industrial'
  density: 0 | 1 | 2 | 3       // 0 = empty zoned cell, 3 = max
  row: number
  col: number
  // grown building shape derives from kind + density at render time
}

City.buildings now includes Zone in addition to the v1 placeholder types.
```

## Out of scope for this section

- Mixed-use zoning (a single cell that is both residential and commercial). Not in SimCity 2000 either.
- Zone density paint (low / medium / high). v1 grows organically from low to high.
- Per-building decoration variants. The grown building visual is determined by kind + density only.

### Build log

- 2026-05-12: REQ-081 per-cell power gate landed (the first of the per-cell supply / demand gates promised in the GDD coverage row). Files: `src/lib/sim/events.ts` (`maybeGrowZones(zones, tick, cityHappiness, powerStatus)` signature gains a fourth `powerStatus: Record<string, CellPowerStatus>` argument; the happy-band branch now skips cells whose status is `'brownout'` or `'unpowered'`; a missing entry (undefined) falls through as "no gate" so a pre-electrical city grows as before; doc comment rewritten to describe the gate and call out the back-compat path; `applyTick` imports `solvePowerStatus` from `./powerSolver`, short-circuits the solve when `power.plants.length === 0 && Object.keys(power.lines).length === 0`, otherwise threads the resolved map into `maybeGrowZones`). Tests: `tests/lib/sim/events.test.ts` adds 8 new cases under "per-tick zone growth power gating (REQ-081 power gate)" covering: back-compat no-power city grows normally, powered cell advances + unpowered same-bucket cell stalls (event-driven), cutting power mid-game stalls a previously growing cell (line-erase), restoring power resumes a stalled cell, direct-call miserable-band ignores power status, direct-call happy-band mixed-bucket per-cell gating, brownout status blocks growth identity. Verified `npm run check:dashes`, `npm run type-check`, `npm test` (2535/2535), `npm run build` green. The demo city payoff: 10 powered outskirts cells (rows 1, 9) advance toward density 3 while the 12 unpowered interior cells inside the loop ring hold at their current densities. Water (REQ-090) and services (REQ-100) per-cell gates stay deferred to follow-on slices. PR #TBD.
- 2026-05-06: REQ-081 + REQ-079 three-tier growth / decline landed. Files: `src/lib/sim/state.ts` (new `DECLINE_HAPPINESS_THRESHOLD = 25` constant with header comment block tying it to the [0, 100] happiness range and explaining the stagnant-band rationale), `src/lib/sim/events.ts` (`maybeGrowZones` doc comment rewritten to describe the three bands; the function now branches on `cityHappiness <= DECLINE_HAPPINESS_THRESHOLD` (decline: density-greater-than-0 cells step down by 1, floored at 0, cells stay zoned at density 0), `cityHappiness > GROWTH_HAPPINESS_THRESHOLD` (grow: density-<3 cells advance by 1), or otherwise stagnant identity; the same `changed` flag tracks whether the bucket needs to be replaced so an all-at-cap (or all-at-0-in-decline) pass returns the input bucket reference). Tests: `tests/lib/sim/events.test.ts` reworks two pre-existing cases to use a treatment plant + tax 35% (drained sewage means happiness is a stable 30 sitting in the stagnant band, not slowly declining as waste accumulates), adds a new "density steps DOWN when cityHappiness drops to the miserable band" case (tax 50% drives happiness to 0, density 1 -> 0 on tick 40), adds a direct-call `maybeGrowZones` unit case proving density-0 cells in the miserable band produce identity (no negative densities). Verified `npm test` (2136/2136), `npm run build`, `npm run check:dashes`, `git diff --check`, `npx playwright test e2e/sim.spec.ts --project=chromium` (25/25). The intended player feedback: drop happiness past 50 -> growth stalls; drop further past 25 -> residents abandon. Cells stay zoned at density 0 so a player who restores happiness sees their layout grow back instead of having to repaint zones. PR #N.
- 2026-05-06: REQ-081 "growth stalled" HUD indicator landed. Files: `src/app/[slug]/edit/EditorClient.tsx` (imports `GROWTH_HAPPINESS_THRESHOLD` from `src/lib/sim/state.ts`; new inline span next to the existing `editor-sim-happiness` readout that mounts whenever `simState.population.cityHappiness <= GROWTH_HAPPINESS_THRESHOLD`; carries `data-testid="editor-sim-growth-stalled"` and `data-sim-growth-stalled="true"` plus the existing low-happiness red color so the visual cue is consistent). Tests: `e2e/sim.spec.ts` adds 1 new case "growth-stalled indicator appears once happiness drops to GROWTH_HAPPINESS_THRESHOLD" that asserts the indicator is absent on a fresh 100-happiness city, paints a residential cell at origin, runs at 4x for ~5s while waste accumulates, then waits for the indicator to mount. Verified `npm test` (2134/2134), `npm run build`, `npm run check:dashes`, `git diff --check`, `npx playwright test e2e/sim.spec.ts --project=chromium` (25/25). Closes the visibility gap from PR #131: the gate's effect is now visible in the HUD instead of only inferable by watching density not advance. PR #N.
- 2026-05-06: REQ-081 happiness-gated zone growth landed (slice 2 of 2 in the original "slice 1 of 2" plan; happiness was the first growth-gating signal anticipated by that plan). Note: per-cell supply / demand gating from power / water / services layers may land as additional follow-on slices later, which would extend the slice count beyond 2; the original "slice 1 of 2" label was the smaller framing of the time. Files: `src/lib/sim/state.ts` (new `GROWTH_HAPPINESS_THRESHOLD = 50` constant with header comment block tying it to the [0, 100] happiness range and explaining the starter-city-stays-at-100 default-grows-out-of-the-box reasoning), `src/lib/sim/events.ts` (`maybeGrowZones(zones, tick, cityHappiness)` signature extended with the third happiness argument; the helper short-circuits to `return zones` when `cityHappiness <= GROWTH_HAPPINESS_THRESHOLD` so an unhappy city stagnates at its current density mix; `applyTick` reads `state.population.cityHappiness` (pre-tick value) and threads it into the call site so the previous tick's happiness decides this tick's growth; doc comment on `maybeGrowZones` updated to call out the gate and to note that per-cell supply / demand gating from power / water / services layers stays a follow-on slice). Tests: `tests/lib/sim/events.test.ts` two new cases under "per-tick zone growth (REQ-081 slice 1)" covering: tax-induced unhappy city stalls at density 1 (residential tax raised to 0.5 keeps post-tick-20 happiness at or below 50, so tick 40 is a no-op and the zones bucket reference is preserved), and a tax-cut path resumes growth (player drops tax back to default 0.07 mid-game, happiness recomputes the next tick, the next growth tick advances density 1 -> 2). Verified `npm test` (2134/2134), `npm run build`, `npm run check:dashes`, `git diff --check`, `npx playwright test e2e/sim.spec.ts --project=chromium` (24/24). Closes the multi-input happiness loop landed in PR #130: tanking happiness through high taxes / untreated waste / missing services / active disasters now stalls organic density growth instead of being a HUD-only readout. Per-cell supply / demand gating, per-cell happiness, and a "growth stalled" HUD indicator all stay deferred. PR #131.
- 2026-05-06: REQ-082 R/C/I demand HUD readouts landed. Files: `src/lib/sim/rciDemand.ts` (new pure module: `computeRciDemand(zones, population)` returns signed integer deltas: `residential = (commercialJobs + industrialJobs) - residents` (positive when there are more jobs than residents), `commercial = residents - commercialJobs` (positive when residents need shopping), `industrial = residents - industrialJobs` (positive when residents need work); job counts come from the existing `COMMERCIAL_JOBS_BY_DENSITY` / `INDUSTRIAL_JOBS_BY_DENSITY` tables), `src/app/[slug]/edit/EditorClient.tsx` (HUD row gains 3 demand readouts next to the population span: each shows R/C/I + signed integer delta with red/green coloring (red negative, green positive), mirrors `data-sim-demand` per-kind for e2e selection). Tests: `tests/lib/sim/rciDemand.test.ts` ships 8 cases covering: empty city = all zeros, jobs > residents drives positive R demand, residents > 0 with no jobs drives negative R demand, residents > commercial-jobs drives positive C demand, residents > industrial-jobs drives positive I demand, residential density does not feed jobs, density-3 commercial / industrial scales jobs by the density table (commercial 30 / industrial 35), two-replay determinism. `e2e/sim.spec.ts` adds 1 new case "R/C/I demand readouts show signed deltas" that asserts the three readouts start at 0 on an empty city, paints an industrial zone, runs at 4x for ~5s to grow it to density 1, asserts the residential demand readout flips positive (industrial jobs > 0 residents). Verified `npm run type-check`, `npm test` (2131/2131), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 24/24 local. v1 ships raw signed integer deltas; a future polish slice could add visual bar widgets clamped -1..+1 like SimCity's classic RCI bar. PR #N.
- 2026-05-06: REQ-083 commercial / industrial revenue landed (jobs feed the economy reducer's per-tick income alongside residential residents). Files: `src/lib/sim/state.ts` (new `COMMERCIAL_JOBS_BY_DENSITY` and `INDUSTRIAL_JOBS_BY_DENSITY` per-density tables: commercial 0/3/10/30, industrial 0/4/14/35; commercial sits slightly under residential because jobs travel and residents sleep, industrial sits slightly above commercial because industrial cells are larger employers per footprint), `src/lib/sim/events.ts` (`applyEconomyTick` signature gains a `zones: ZonesBucket` parameter; the income calc now sums commercial-jobs-per-density times `taxRates.commercial` plus industrial-jobs-per-density times `taxRates.industrial` alongside the existing residential branch; `applyTick` threads `nextZones` through). v1 has no employment-matching reducer so every job slot is treated as filled regardless of total population; a future "jobs-need-residents" gate could land in a follow-on slice. Tests: `tests/lib/sim/events.test.ts` adds 4 cases under "commercial / industrial revenue (REQ-083 slice 1)" covering: commercial density 1 generates 3 jobs * 0.07 = 0.21/tick, industrial density 1 generates 4 jobs * 0.05 = 0.20/tick, residential + commercial + industrial all stack additively (0.28 + 0.21 + 0.20 = 0.69/tick), two-replay determinism over a mixed-zone event log. Two existing direct-call tests of `applyEconomyTick` updated to pass the new 5th `zones` argument. Verified `npm run type-check`, `npm test` (2081/2081), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 22/22 local. REQ-082 R/C/I demand bars stay deferred. PR #N.
- 2026-05-06: REQ-080 + REQ-110 unification landed (the editor IS the sim editor). Files: `src/app/[slug]/edit/editorState.ts` (extended `PaletteCategory` from `'street' | 'building'` to `'street' | 'building' | 'zone'`; added `ZonePaletteEntry` type, `ZONE_PALETTE` constant matching `ZoneKind` (residential / commercial / industrial), `DEFAULT_ZONE_TYPE = 'residential'`), `src/app/[slug]/edit/EditorClient.tsx` (added `builderId` prop, mounted `useSimEngine`, added `selectedZoneType` state, extended the category-switcher tablist to include the zone tab, added a per-tab palette branch rendering the zone buttons with per-kind palette colors when zone category is active, extended `handleCellClick` to dispatch strict `EraseZoneEvent` / `PlaceZoneEvent` via `simEngine.enqueue` when zone category is active, added a sim-speed toolbar block above the existing tools toolbar with Pause / 1x / 2x / 4x buttons and a tick readout, passed `simState.zones` to the `SnapGrid` component as a new optional prop), `src/app/[slug]/edit/SnapGridView.tsx` (added optional `zones?: ZonesBucket | null` prop; when supplied, every cell carries `data-cell-zoned` / `data-cell-zone-kind` / `data-cell-zone-density` attributes mirroring the SimGridView convention; renders a translucent `editor-zone-overlay` `<rect>` per zoned cell after the cells block but before the open-end overlays so the place / erase ghosts and connector glyphs stay legible over the zoned background), `src/app/[slug]/edit/page.tsx` (read builder cookie via `cookies()`, falls back to `notFound()` if missing per the same pattern the sim view used; passes `builderId` to `EditorClient`), `src/app/[slug]/sim/page.tsx` (rewrote from a separate SimViewClient mount to a 308 redirect to `/<slug>/edit`; preserves any external bookmarks while making the editor the canonical surface), deleted `src/app/[slug]/sim/SimGridView.tsx` and `src/app/[slug]/sim/SimViewClient.tsx` (no longer needed; the editor's SnapGridView now handles zone rendering and the editor's toolbar carries the speed controls). Tests: rewrote `e2e/sim.spec.ts` from 5 cases on the legacy SimViewClient to 6 cases on the unified editor: legacy redirect, editor toolbar exposes Pause / 1x / 2x / 4x speed controls, Pause stops the tick, 1x advances ticks, switching to Zones tab exposes the zone palette and paints a residential zone, switch zone kind to commercial then erase mode then click to remove. Existing `e2e/editor.spec.ts` 28 cases continue to pass; the new third tab does not break the existing two-tab assertions because they target the active tab's `aria-selected` state rather than the tab list's length. Verified `npm run type-check`, `npm test` (1811/1811), `npm run build` (sim route gone; editor route grew from 7.79kB to ~9.3kB First Load JS reflecting the absorbed simEngine + zone palette), `npm run check:dashes`, `git diff --check`, `npx playwright test --project=chromium` 41/42 (the 1 failure is a pre-existing local-only KV state issue in `home.spec.ts:12` that fires on main too; CI passes). Architectural rationale: the user pointed out that the editor and the sim view are the same kind of surface (paintable grid with place / erase tools). Splitting them across two routes was a tactical choice during the sim prototype that left two parallel surfaces with overlapping concepts. Unifying clarifies the surface, removes UI duplication, and makes the sim layer feel like a first-class editor concept rather than a separate mode. PR #N.
- 2026-05-06: REQ-081 per-tick zone growth landed (slice 1 of 2; deterministic unconditional growth). Files: `src/lib/sim/state.ts` (new `GROWTH_INTERVAL_TICKS = 20` constant; at the 4Hz default that is 5 seconds of wall time per density step and ~15 seconds for a fresh zone to reach max density 3; speed multipliers compress wall-clock pace proportionally because every tick is processed identically), `src/lib/sim/events.ts` (extended `applyTick` to compute the next tick first, then call new exported pure helper `maybeGrowZones(zones, nextTick)` that returns the bucket unchanged when the tick is not a growth tick OR every cell is already at max density, and a fresh bucket with every density-<3 cell advanced by 1 otherwise; the helper iterates `Object.keys(zones.cells)` and tracks a `changed` flag so an all-at-cap pass returns the same reference and the engine's downstream consumers see identity-on-no-change). Tests: `tests/lib/sim/events.test.ts` added 9 new cases under "per-tick zone growth (REQ-081 slice 1)" covering: pre-first-growth-tick density 0 (ticks 1..19), exactly-at-first-growth-tick density 1 (tick 20), between-ticks density preservation (ticks 21..39), continued-growth at next interval (40 -> 2, 60 -> 3), max-density cap with identity-bucket-preservation, multi-cell synchronized growth, mixed-density cap-and-skip behavior, paused sim does not grow, and two-replay determinism over a 20-tick event log. Verified `npm run type-check`, `npm test` (1811/1811), `npm run build`, `npm run check:dashes`, `git diff --check` all green. The visible payoff: paint a residential zone, watch its opacity climb from 0.35 (density 0) to 1.0 (density 3) over ~15 seconds at 1x. Slice 2 of REQ-081 (demand-gated growth, gating on citizens / power / water / services as those layers ship) lands later; v1 unconditional growth keeps the loop visibly alive in the meantime. PR #N.
- 2026-05-06: REQ-080 zoning slice 2 of N landed (clickable grid + zone palette in the sim view). Files: `src/app/[slug]/sim/SimGridView.tsx` (new client component rendering a flat top-down 2D SVG grid; reuses the editor's `GRID_RADIUS` / `CELL_PIXELS` / `cellToPixel` / `gridCells` helpers from `../edit/snapGrid` so the sim view paints on the same coordinate space as the editor and a future REQ-110 step-2 merge of the two views lands without coordinate translation; renders zoned cells from `state.zones.cells` with a per-kind fill (`ZONE_FILL` map: residential green, commercial blue, industrial brown) and per-density opacity (`ZONE_DENSITY_OPACITY` map: 0.35 / 0.55 / 0.78 / 1.0 so the player sees ungrown vs grown at a glance), each cell carries `data-cell-zoned` / `data-cell-zone-kind` / `data-cell-zone-density` attributes for e2e selection, the SVG root mirrors `data-zone-count` and `data-cursor-mode`, the cursor swaps between crosshair / not-allowed by `cursorMode` to mirror the editor's place / erase tool vocabulary), `src/app/[slug]/sim/SimViewClient.tsx` (added `ZoneTool = ZoneKind | 'erase'` union, `ZONE_TOOLS` constant with four entries (residential / commercial / industrial / erase) carrying their display label and palette color, `tool` useState defaulting to residential, `handleCellClick` callback that dispatches a strict `EraseZoneEvent` when the erase tool is selected and a strict `PlaceZoneEvent` otherwise via `engine.enqueue`; new `sim-zone-palette` section with the four tool buttons mirroring the existing speed buttons' aria-pressed pattern; new `sim-grid-section` mounting the SimGridView with the live zones bucket and the cursorMode derived from the active tool). Tests: `e2e/sim.spec.ts` (added 2 new playwright cases under the existing describe: "zone palette + grid: painting a residential zone updates the cell" pauses the sim, asserts residential is the default active tool, clicks the origin cell, asserts the cell flips to zoned with kind='residential' density='0', confirms the grid root mirrors zone-count='1'; "zone palette: switching tools and erasing removes a zone" switches to commercial, paints (1,1), asserts the cell carries the commercial kind, switches to erase, clicks the same cell, asserts the cell unzones and the count drops to 0). Verified `npm run type-check`, `npm test` (1802/1802, no new unit cases; the UI is exercised by e2e), `npm run build` (sim route bundle grew from 3.29kB to 4.17kB with the grid + palette), `npm run check:dashes` clean, `git diff --check` clean, `npx playwright test e2e/sim.spec.ts --project=chromium` 5/5 local. The tools section pre-densifies the React tree for follow-on slices: power tools (REQ-085), water tools (REQ-090), service buildings (REQ-100), and the toolbar tab structure (REQ-112) plug into the existing palette layout pattern. Per-tick growth (REQ-081), R/C/I demand bars (REQ-082), and job slots (REQ-083) stay deferred to their own dots. PR #N.
- 2026-05-06: REQ-080 zoning slice 1 of N landed (schema + placeZone/eraseZone events + reducer; first sim layer to plug into the substrate). Files: `src/lib/sim/state.ts` (tightened the `ZonesBucketSchema` from `z.object({}).passthrough()` to a strict `{ cells: Record<string, ZoneCell> }` shape; new `ZoneKindSchema` enum (residential / commercial / industrial), `ZoneDensitySchema` union (0..3), `ZoneCellSchema` strict `{ kind, density }`, `EMPTY_ZONES_BUCKET = { cells: {} }` frozen at module load, `zoneCellKey(row, col)` returning the `"row,col"` string matching the existing `streetCellSet` / `buildingCellSet` convention; `EMPTY_SIM_STATE.zones` now points at `EMPTY_ZONES_BUCKET` instead of the raw `{}`), `src/lib/sim/events.ts` (new strict `PlaceZoneEventSchema` and `EraseZoneEventSchema` carrying the `(kind, row, col)` and `(row, col)` payloads; added both to the `SimEventSchema` discriminated union; removed `placeZone` and `eraseZone` from the `PlaceholderLayerEventSchema` enum so the typed variants are routed there instead; added `applyPlaceZone` and `applyEraseZone` reducer cases to the `applySimEvent` dispatch; `applyPlaceZone` returns identity when the same kind is painted on an already-zoned cell, overwrites the kind on a retype, and preserves the existing density on retype so the per-tick growth reducer (REQ-081, follow-on slice) does not get reset by an editing player; `applyEraseZone` returns identity when the targeted cell is not zoned). The spec text in REQ-084 originally placed zones on the existing `city.buildings` array as a discriminated union; after Q-012 (event sourcing) resolved, the cleaner home is `city.sim.zones.cells` because the reducer is scoped to mutate sim state only. The legacy `city.buildings` array stays untouched for v1 placeholder buildings (small-house etc.); a future migration slice can lift those into the zone system when the visual divergence becomes a felt gap. Tests: `tests/lib/sim/state.test.ts` (added 25 new cases under ZoneKindSchema / ZoneDensitySchema / ZoneCellSchema / ZonesBucketSchema / EMPTY_ZONES_BUCKET / zoneCellKey covering accepts the three v1 kinds, rejects unknown kinds, density 0..3 accept and out-of-range reject, strict ZoneCell with bad-extras rejection, ZonesBucket with populated-cells acceptance and bad-kind rejection, EMPTY_ZONES_BUCKET shape and frozen invariant, zoneCellKey for zero / negative / convention-match), `tests/lib/sim/events.test.ts` (added 14 new cases: `placeZone` adds new cell at density 0, identity-on-same-kind-same-cell, kind-overwrite-on-retype, density-preserved-on-retype with a forged density-2 starting point, no-mutation, multi-cell-independent-positions, negative-coords; `eraseZone` identity-on-fresh-slug, removes-targeted-cell, only-removes-targeted, identity-on-nonexistent; `reduceSimEvents` over the full zoning vocabulary covering replay determinism and two-builder-id convergence; renamed the layer-specific forward-compat block to drop the placeZone test case since it is now strict). Verified `npm run type-check`, `npm test` (1802/1802), `npm run build`, `npm run check:dashes`, `git diff --check` all green. REQ-080 flips not_started to partial. The zone-painting UI (REQ-082 demand bars, the toolbar tab from REQ-112), per-tick growth reducer (REQ-081), and citizen demand integration (REQ-082) land in follow-on slices. PR #N.
