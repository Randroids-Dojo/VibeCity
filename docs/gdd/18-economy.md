# 18. Economy

**Status:** partial

The economy layer is the money pressure that keeps the player from infinite-zoning. Citizens and businesses pay taxes, infrastructure costs to build and to maintain, services cost per-tick. Bankruptcy ends the session (player must reset) but does not destroy the slug.

This file is the canonical spec for the economy layer (REQ-095 through REQ-099).

## What this section covers

- **REQ-095** Tax revenue. Each populated residential / commercial / industrial cell pays per-tick tax based on density and a per-zone-type tax-rate slider. Default rates: residential 7%, commercial 7%, industrial 5%.
- **REQ-096** Build cost. Placing infrastructure (streets, plants, lines, pipes, buildings) deducts a one-time cost from the treasury. Costs scale with multi-cell footprint.
- **REQ-097** Maintenance cost. Each placed infrastructure cell costs per-tick maintenance. A power plant with broken transmission still costs maintenance.
- **REQ-098** Treasury HUD. Top-of-screen readout: current treasury, monthly net income / loss, tax-rate sliders. Always visible in sim view; collapsed in drive view.
- **REQ-099** Bankruptcy. Treasury below zero for 30 in-game days ends the session: a banner offers "reset budget" (treasury restored, infrastructure unchanged) or "reset city" (slug returns to empty).

## Schema sketch

```ts
City.sim.economy = {
  treasury: number
  taxRates: { residential: number; commercial: number; industrial: number }
  monthlyIncome: number
  monthlyMaintenance: number
  bankruptcyDaysRemaining: number  // counts down from 30 when treasury < 0
}
```

## Out of scope for this section

- Loans, bonds, debt instruments (defer to v1.1).
- Per-building specific costs beyond zone-type defaults.
- Inflation, interest rates.

### Build log

- 2026-05-06: REQ-095 slice 5 of N landed (tax-rate HUD sliders for residential / commercial / industrial). Files: `src/app/[slug]/edit/EditorClient.tsx` (the bankruptcy / treasury HUD row gains 3 slider widgets, one per zone kind: each is a labelled R/C/I prefix + minus button + percent readout + plus button. Click dispatches a `setTaxRate` event with the new rate (1pp increments, clamped 0..100%). The widget mirrors the per-kind rate via `data-sim-tax-rate` for e2e selection. The minus button uses the Unicode "minus sign" (U+2212), distinct from U+2013 / U+2014 dashes that the dash-check script blocks). Tests: `e2e/sim.spec.ts` adds 1 new case "residential tax HUD slider adjusts the per-tick income" that asserts the residential rate readout starts at 0.07, clicks the up button (bumps to 0.08), clicks down twice (clamps at 0.06). Verified `npm run type-check`, `npm test` (2123/2123 unit pass; no new unit cases since the slice is pure UI), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 23/23 local. With slice 5, the player can adjust tax rates from the HUD; the next economy slice would surface a per-tick net-income readout (income minus maintenance) as a forecasting cue. PR #N.
- 2026-05-06: REQ-095 slice 4 of N landed (resetBudget + resetCity events + HUD buttons; commercial / industrial revenue REQ-083 still outstanding). Files: `src/lib/sim/events.ts` (new strict `ResetBudgetEventSchema` and `ResetCityEventSchema` with empty payloads, both routed in the discriminated union; `applyResetBudget` reducer restores `economy` to `EMPTY_ECONOMY_BUCKET` (treasury = 20000, lastTickIncome = 0, lastTickMaintenance = 0, bankruptcyTickCounter = 0) while keeping every other layer's state intact so the player keeps their built infrastructure; `resetCity` dispatch maps directly to `EMPTY_SIM_STATE` so the entire sim resets; pre-pivot `City.pieces` / `City.buildings` are NOT touched because they live outside the sim engine), `src/app/[slug]/edit/EditorClient.tsx` (when the bankruptcy warning span shows, two new buttons mount alongside it: `editor-sim-reset-budget` (green) restores treasury / clears the counter, `editor-sim-reset-city` (red) resets the entire sim state). Tests: `tests/lib/sim/events.test.ts` adds 5 cases under "resetBudget + resetCity (REQ-095 slice 4)" covering: resetBudget restores INITIAL_TREASURY, resetBudget zeros bankruptcyTickCounter, resetBudget preserves placed infrastructure (other layers untouched), resetCity returns the EMPTY_SIM_STATE reference, two-replay determinism over an event log including resetBudget. `e2e/sim.spec.ts` adds 1 new case "bankruptcy reset-budget button restores treasury to 20000" that drives treasury negative with 6 coal plants, ticks once at 4x to mount the warning span, clicks the reset-budget button, asserts treasury reads 20000 and the warning span unmounts. Verified `npm run type-check`, `npm test` (2077/2077), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 22/22 local. Slice 5+ lands commercial / industrial revenue (REQ-083 dependency) and possibly an automatic `resetCity` event triggered when the bankruptcy counter hits the threshold (currently the sim runs past the deadline indefinitely; the player must click). PR #N.
- 2026-05-06: REQ-095 slice 3 follow-up (Copilot review fixes on PR #113). The earlier entry below claimed the identity-on-no-change branch in `applyEconomyTick` skips per-tick allocations during long steady-state deficits; that claim was inaccurate at the time the entry was written because the counter incremented every deficit tick, so the bucket changed every tick. Fix: cap `nextBankruptcyCounter` at `BANKRUPTCY_THRESHOLD_TICKS` via `Math.min(...)` so a long deficit hits a stable saturated value and the identity branch fires correctly. Also fixed: HUD seconds calculation now scales with `simState.speed` via `TICK_INTERVAL_MS_BASE` (previously hard-coded `/4`, which read wrong at 1x and 2x). Misleading test "counter resets to 0 when treasury rebounds above 0" was renamed to "counter saturates at BANKRUPTCY_THRESHOLD_TICKS after long deficit" and a new "saturated counter lets the per-tick reducer short-circuit identity" case proves the cap behavior. Verified `npm test` (2017/2017), `npm run build`, `npm run check:dashes` all green. PR #113.
- 2026-05-06: REQ-095 economy slice 3 of N landed (bankruptcy countdown HUD; reset-budget / reset-city events defer to slice 4). Files: `src/lib/sim/state.ts` (new `BANKRUPTCY_THRESHOLD_TICKS = 240` (60 seconds at default 4Hz); `EconomyBucketSchema` extended with `bankruptcyTickCounter: z.number().int().min(0)`; `EMPTY_ECONOMY_BUCKET` seeds it 0), `src/lib/sim/events.ts` (`applyEconomyTick` now increments `bankruptcyTickCounter` whenever the freshly computed `nextTreasury` is below 0 and resets it to 0 when treasury rebounds to non-negative; identity-on-no-change now also compares the counter so a long deficit run does not allocate per tick once income / maintenance steady-state), `src/app/[slug]/edit/EditorClient.tsx` (HUD adds an `editor-sim-bankruptcy-warning` span next to the happiness readout when `bankruptcyTickCounter > 0`; renders "bankrupt in Ns" where N counts down from 60s based on the threshold; styled red and bold to read as urgent). Tests: `tests/lib/sim/state.test.ts` updates the empty-economy-bucket shape assertion to include `bankruptcyTickCounter: 0`. `tests/lib/sim/events.test.ts` adds 5 cases under "bankruptcy countdown (REQ-095 slice 3)" covering: counter is 0 when treasury is positive, counter increments per tick once treasury drops below 0, counter still 0 immediately after a placement that drove treasury negative (no tick has fired yet), `applyEconomyTick` directly resets the counter when given a non-negative-treasury start, two-replay determinism over a long-deficit event log. `e2e/sim.spec.ts` adds 1 new case "bankruptcy warning fires once treasury drops below 0" that places six coal plants ($24,000 vs $20,000 starter) so treasury goes to -4000, unpauses to 4x, asserts the warning span becomes visible. Verified `npm run type-check`, `npm test` (2016/2016), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 20/20 local. Slice 4 lands `resetBudget` and `resetCity` events that fire when the countdown hits 0 (resetBudget zeroes treasury back to INITIAL_TREASURY but keeps the city; resetCity resets the whole sim state). Commercial / industrial revenue (REQ-083) still pending. PR #N.
- 2026-05-06: REQ-095 economy slice 2 of N landed (build cost deducted from treasury on placement; bankruptcy countdown defers to slice 3). Files: `src/lib/sim/state.ts` (new build-cost constants: `POWER_PLANT_BUILD_COST` (coal 4000, solar 2500), `POWER_LINE_BUILD_COST = 5`, `ZONE_BUILD_COST` (residential 50, commercial 75, industrial 100), `WATER_SOURCE_BUILD_COST` (water-tower 400, pump-station 1500), `WATER_PIPE_BUILD_COST = 5`, `SEWAGE_TREATMENT_BUILD_COST = 2500`, `SERVICE_BUILD_COST` per-kind (police 500, fire 500, hospital 1500, school 750, garbage 400)), `src/lib/sim/events.ts` (new private `chargeBuild(state, cost)` helper that returns a fresh state with `economy.treasury` decremented; treasury is allowed to go negative because the bankruptcy countdown in slice 3 reads the running balance; wired into seven placement reducers: `applyPlaceZone` charges by kind on a fresh zoning OR a retype to a different kind, `applyPlacePowerPlant` charges by plant kind, `applyRunPowerLine` flat per cell, `applyPlaceServiceBuilding` by service kind, `applyPlaceWaterSource` by source kind, `applyRunWaterPipe` flat per pipe cell, `applyPlaceSewageTreatmentPlant` flat; the existing identity-on-no-change branches in each reducer skip the charge so a duplicate-anchor click does not bill the player twice). Existing maintenance-tick assertions updated for the new build-cost-aware starting balance (`19999.95` -> `19994.95` after the line cost; `19999.5` -> `15999.5` after the coal plant cost). Tests: `tests/lib/sim/events.test.ts` adds 7 cases under "build cost on placement (REQ-095 slice 2)" covering: residential zone deducts 50, coal plant deducts 4000, line deducts 5, hospital deducts 1500, sewage plant deducts 2500, duplicate-anchor place returns identity (no double charge), six coal plants drives treasury negative (proves negative balance is allowed). `e2e/sim.spec.ts` updates the existing treasury-decreases case: now asserts treasury drops from 20000 to 16000 immediately on coal-plant placement (build cost), then continues to drop below 16000 as maintenance ticks fire. Verified `npm run type-check`, `npm test` (2011/2011), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 19/19 local. Slice 3 lands the bankruptcy countdown (treasury below 0 for N consecutive ticks shows a "Bankruptcy in N seconds" warning then offers reset budget / reset city). Commercial / industrial revenue waits on REQ-083 job slots. PR #N.
- 2026-05-06: REQ-095 economy slice 1 of N landed (treasury + per-tick income/maintenance + HUD readout). Files: `src/lib/sim/state.ts` (tightened `EconomyBucketSchema` from passthrough to strict `{ treasury, lastTickIncome, lastTickMaintenance }`; new `INITIAL_TREASURY = 20000`, `LINE_MAINTENANCE_PER_TICK = 0.05`, `PLANT_MAINTENANCE_PER_TICK = 0.5` constants; `EMPTY_ECONOMY_BUCKET` frozen with treasury at INITIAL_TREASURY), `src/lib/sim/events.ts` (extended `applyTick` to call new exported pure helper `applyEconomyTick(economy, population, power, taxRates)`; income = `population.totalPopulation * taxRates.residential` per tick, maintenance = line count * 0.05 + plant count * 0.5; treasury accumulates the net delta; identity-on-no-change short-circuits when income, maintenance, and treasury all match the input bucket so a static city does not allocate per tick), `src/app/[slug]/edit/EditorClient.tsx` (added `editor-sim-treasury` readout span next to the population readout; renders `$N,NNN` formatted with `toLocaleString('en-US')`; turns red when treasury < 0). Tests: `tests/lib/sim/state.test.ts` adds 1 case for the strict `EMPTY_SIM_STATE.economy` shape (treasury 20000, income 0, maintenance 0). `tests/lib/sim/events.test.ts` adds 8 cases under "per-tick economy (REQ-095 slice 1)" covering: treasury starts at INITIAL_TREASURY, paused sim does not change treasury, no-infrastructure tick = no income / no maintenance, single line maintenance = 0.05/tick, single coal plant maintenance = 0.5/tick, residents generate income at residential rate (4 residents at 7% = 0.28/tick), two-replay determinism, setTaxRate flips income on next tick. `e2e/sim.spec.ts` adds 1 new case "treasury starts at 20000 and decreases as power infrastructure is placed" that pauses, asserts treasury=20000, places a coal plant (no tick fires while paused so treasury stays at 20000), unpauses to 4x, polls treasury via expect.poll until it drops below 20000 (proves the per-tick maintenance loop runs against the placed plant). Verified `npm run type-check`, `npm test` (1912/1912), `npm run build`, `npm run check:dashes`, `git diff --check` all green; `npx playwright test e2e/sim.spec.ts --project=chromium` 12/12 local. Slice 2 of REQ-095 lands the build-cost deduction on infrastructure placement and the bankruptcy countdown (treasury < 0 for N consecutive ticks shows a "Bankruptcy in N seconds" warning then offers reset budget / reset city). Commercial / industrial revenue depends on REQ-083 job slots; ships when those land. PR #N.
